---
phase: 10-inline-quantity-controls
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/lib/components/items/ItemInput.svelte
  - src/lib/components/barcode/BarcodeLookupSheet.svelte
  - src/routes/(protected)/lister/[id]/+page.svelte
  - src/lib/queries/items.ts
  - tests/items.spec.ts
  - tests/barcode.spec.ts
  - tests/mobile-layout.spec.ts
autonomous: true
requirements:
  - LIST-08

must_haves:
  truths:
    - "Typed add starts from a visible default quantity of 1 instead of a blank/null quantity state"
    - "Barcode-assisted add also persists quantity 1 unless the user changes it before confirming"
    - "New-item creation paths share one default-quantity rule so future remembered-item entry can inherit it cleanly"
    - "Phase 10 includes focused evidence for both inline quantity interactions and default quantity behavior"
  artifacts:
    - path: "src/lib/components/items/ItemInput.svelte"
      provides: "Add bar with a visible mini stepper defaulting to quantity 1"
      contains: "1"
    - path: "src/lib/components/barcode/BarcodeLookupSheet.svelte"
      provides: "Barcode confirmation flow aligned to the same default quantity rule"
    - path: "src/routes/(protected)/lister/[id]/+page.svelte"
      provides: "Shared add-flow orchestration for typed and barcode-assisted item creation"
    - path: "tests/items.spec.ts"
      provides: "Typed add and inline quantity regression coverage"
    - path: "tests/barcode.spec.ts"
      provides: "Barcode-assisted add default quantity coverage"
    - path: "tests/mobile-layout.spec.ts"
      provides: "Narrow-viewport guard against stepper-induced overflow"
  key_links:
    - from: "src/lib/components/items/ItemInput.svelte"
      to: "src/routes/(protected)/lister/[id]/+page.svelte"
      via: "visible default quantity control passed into the shared add handler"
      pattern: "onAdd"
    - from: "src/lib/components/barcode/BarcodeLookupSheet.svelte"
      to: "src/routes/(protected)/lister/[id]/+page.svelte"
      via: "confirmation flow using the same quantity default contract"
      pattern: "onConfirm"
---

<objective>
Normalize all current new-item entry paths around a visible default quantity of `1`, then add the focused verification coverage required to prove the Phase 10 behavior on desktop and mobile-sized screens.

Purpose: Satisfy LIST-08 and close the phase with evidence. Typed add and barcode-assisted add must behave the same way today, while future suggestion entry in Phase 11 should inherit the same quantity default automatically.

Output: The add bar uses a visible mini stepper starting at `1`, barcode confirmation persists the same default unless changed, and the targeted E2E suite covers the new quantity behavior.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-inline-quantity-controls/10-CONTEXT.md
@.planning/phases/10-inline-quantity-controls/10-RESEARCH.md
@.planning/phases/10-inline-quantity-controls/10-VALIDATION.md
@.planning/phases/10-inline-quantity-controls/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize typed and barcode-assisted add flows around default quantity 1</name>
  <files>src/lib/components/items/ItemInput.svelte, src/lib/components/barcode/BarcodeLookupSheet.svelte, src/routes/(protected)/lister/[id]/+page.svelte, src/lib/queries/items.ts</files>
  <action>
Replace the add-bar quantity field with a visible mini stepper that starts at `1`, and align the barcode confirmation flow to the same default quantity contract.

Requirements:
- Typed add starts from visible quantity `1`
- Barcode-assisted add persists quantity `1` unless the user changes it before confirmation
- Shared item creation behavior should no longer rely on blank quantity mapping to `null`
- Keep the barcode confirmation sheet editable; users can still change quantity before confirming
- Do not implement remembered-item suggestions in this phase, but leave the default quantity rule reusable for that later flow
  </action>
  <verify>
    <automated>npx playwright test tests/items.spec.ts tests/barcode.spec.ts --workers=1</automated>
  </verify>
  <done>Typed and barcode-assisted add flows now share a visible default quantity of `1` and persist that value correctly unless the user changes it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused verification coverage for inline quantity behavior and default quantity persistence</name>
  <files>tests/items.spec.ts, tests/barcode.spec.ts, tests/mobile-layout.spec.ts</files>
  <action>
Extend Playwright coverage so Phase 10 has direct evidence for both LIST-07 and LIST-08.

Coverage requirements:
- increment from the row stepper updates quantity without checking the item off
- decrement above `1` reduces quantity
- decrement at `1` removes the item
- typed add persists quantity `1` by default using both Enter and button-submit paths
- barcode-assisted add starts at and persists quantity `1`
- mobile-sized list layout with visible steppers does not introduce horizontal overflow

If selectors need implementation support, prefer stable test IDs or role-based targets over brittle text-only locators.
  </action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <done>Phase 10 has automated evidence for inline quantity behavior, default quantity persistence, and mobile-safe row layout.</done>
</task>

</tasks>

<verification>
1. `npx playwright test tests/items.spec.ts tests/barcode.spec.ts tests/mobile-layout.spec.ts --workers=1`
2. `npm run test:e2e`
3. Manual phone/PWA pass: add several items using the mini stepper and confirm the controls stay comfortable to tap without accidental check-off.
</verification>

<success_criteria>
- The add bar shows a visible default quantity of `1`
- Typed and barcode-assisted add flows persist quantity `1` unless changed
- Focused Playwright coverage proves inline quantity and default quantity behavior
- Visible steppers do not reintroduce mobile overflow on the list screen
</success_criteria>
