---
phase: 10-inline-quantity-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/components/items/ItemRow.svelte
  - src/lib/components/items/CategorySection.svelte
  - src/routes/(protected)/lister/[id]/+page.svelte
  - src/lib/queries/items.ts
autonomous: true
requirements:
  - LIST-07

must_haves:
  truths:
    - "Active list rows expose a visible right-side stepper for quantity changes without opening the detail sheet"
    - "Tapping the row body still checks an item off; tapping the stepper does not accidentally toggle completion"
    - "Pressing minus at quantity 1 removes the item directly from the active list"
    - "Inline quantity changes feel immediate and stay aligned with the existing optimistic sync pattern"
  artifacts:
    - path: "src/lib/components/items/ItemRow.svelte"
      provides: "Visible row-level quantity stepper with isolated interactions"
      contains: "quantity"
    - path: "src/lib/components/items/CategorySection.svelte"
      provides: "Pass-through integration from grouped rows to inline quantity actions"
    - path: "src/routes/(protected)/lister/[id]/+page.svelte"
      provides: "List-level orchestration for increment, decrement, and remove-at-one behavior"
    - path: "src/lib/queries/items.ts"
      provides: "Optimistic quantity update/remove path aligned with existing list query behavior"
  key_links:
    - from: "src/lib/components/items/ItemRow.svelte"
      to: "existing row tap / swipe / long-press behavior"
      via: "event isolation on the stepper controls"
      pattern: "stopPropagation"
    - from: "src/routes/(protected)/lister/[id]/+page.svelte"
      to: "src/lib/queries/items.ts"
      via: "row stepper handlers calling optimistic quantity/remove actions"
      pattern: "mutate"
---

<objective>
Add inline quantity editing to active shopping-list rows without breaking the current list interaction model. This plan covers the row-level `- quantity +` UI, optimistic quantity updates, and direct remove-at-one behavior.

Purpose: Satisfy LIST-07. The user wants fast quantity changes from the main list, but the list must remain a shopping list first: row tap still checks off, swipe still deletes, and long-press still opens details.

Output: Active rows expose a visible right-side stepper, quantity changes feel immediate, and decrementing from `1` removes the item directly without opening the detail sheet.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-inline-quantity-controls/10-CONTEXT.md
@.planning/phases/10-inline-quantity-controls/10-RESEARCH.md
@.planning/phases/10-inline-quantity-controls/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an inline quantity action path that preserves optimistic sync</name>
  <files>src/lib/queries/items.ts, src/routes/(protected)/lister/[id]/+page.svelte</files>
  <action>
Introduce a focused inline quantity action path for active list items using the existing optimistic query model.

Requirements:
- Reuse or wrap the current optimistic item update behavior rather than introducing a separate server pattern
- Support increment and decrement from the active list UI
- Branch decrement at quantity `1` into direct removal
- Keep the current check-off, delete, and detail-sheet orchestration intact from the list page
- Do not change the done-item section in this phase
  </action>
  <verify>
    <automated>npx playwright test tests/items.spec.ts --workers=1</automated>
  </verify>
  <done>The list page has a stable inline quantity action path that updates optimistically and removes items at quantity `1`.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild active rows with a visible stepper that does not collide with row tap behavior</name>
  <files>src/lib/components/items/ItemRow.svelte, src/lib/components/items/CategorySection.svelte</files>
  <action>
Refactor active item rows so they render a visible right-side `- quantity +` stepper.

Requirements:
- The row body must still check the item off when tapped
- Stepper button presses must not bubble into row check-off, swipe-delete, or long-press behavior
- The stepper should remain visible on narrow mobile rows without reintroducing horizontal overflow
- Quantity should remain legible even when the item name is long

Preserve:
- swipe-to-delete
- long-press detail-sheet entry
- checkbox role semantics for row selection/check-off
  </action>
  <verify>
    <automated>npx playwright test tests/items.spec.ts tests/mobile-layout.spec.ts --workers=1</automated>
  </verify>
  <done>Active rows expose a visible mobile-friendly stepper and still behave correctly for check-off, swipe, and long-press interactions.</done>
</task>

</tasks>

<verification>
1. `npx playwright test tests/items.spec.ts --workers=1`
2. `npx playwright test tests/mobile-layout.spec.ts --workers=1`
3. Manual narrow-phone pass: tap `+` and `-` repeatedly on active rows and confirm no accidental check-off occurs.
</verification>

<success_criteria>
- Active rows show a visible right-side quantity stepper
- Row taps still check items off independently of the stepper
- Minus at quantity `1` removes the item directly
- Inline quantity changes feel immediate and stay in sync with the existing optimistic list flow
</success_criteria>
