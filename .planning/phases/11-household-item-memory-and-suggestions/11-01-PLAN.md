---
phase: 11-household-item-memory-and-suggestions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260312190000_phase11_household_item_memory.sql
  - src/lib/queries/remembered-items.ts
  - src/lib/queries/items.ts
  - tests/helpers/remembered-items.ts
  - tests/item-memory.spec.ts
autonomous: true
requirements:
  - SUGG-01
  - SUGG-02
  - SUGG-03

must_haves:
  truths:
    - "The app has a household-scoped remembered-item source that survives beyond active list rows and stores the latest known category for each normalized item name"
    - "Remembered-item search returns at most five results, narrows from one typed letter onward, and ranks matching household items deterministically"
    - "Phase 11 does not rely on raw client-side scans of household history to drive suggestions"
    - "The remembered-item data contract is reusable by the list page without duplicating search logic in the UI"
  artifacts:
    - path: "supabase/migrations/20260312190000_phase11_household_item_memory.sql"
      provides: "Dedicated remembered-item table or equivalent RPC-backed source with household scoping, normalization, ranking, and category memory"
      contains: "search_household_item_memory"
    - path: "src/lib/queries/remembered-items.ts"
      provides: "Client-side query wrapper for remembered-item lookup"
    - path: "src/lib/queries/items.ts"
      provides: "Shared add-path hooks needed to refresh remembered-item memory from list mutations"
    - path: "tests/helpers/remembered-items.ts"
      provides: "Admin seed helpers for recurring-item and category-memory fixtures"
  key_links:
    - from: "supabase/migrations/20260312190000_phase11_household_item_memory.sql"
      to: "src/lib/queries/remembered-items.ts"
      via: "RPC or query contract returning normalized remembered suggestions"
      pattern: "rpc"
    - from: "src/lib/queries/items.ts"
      to: "remembered-item source"
      via: "shared add-path memory refresh semantics"
      pattern: "upsert"
---

<objective>
Create the household-scoped remembered-item data source and query layer that Phase 11 needs for typeahead suggestions and category reuse.

Purpose: satisfy the backend/data side of SUGG-01..03 before the UI is added. The data source must remember prior household items by normalized name, preserve the latest known category, and expose a compact ranked search result for the add bar.

Output: Supabase has a remembered-item source plus a search RPC/query contract, and the app has a focused query helper and seed fixtures that downstream plans can build on.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-household-item-memory-and-suggestions/11-CONTEXT.md
@.planning/phases/11-household-item-memory-and-suggestions/11-RESEARCH.md
@.planning/phases/11-household-item-memory-and-suggestions/11-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a household remembered-item source with ranked search and category memory</name>
  <files>supabase/migrations/20260312190000_phase11_household_item_memory.sql, tests/helpers/remembered-items.ts, tests/item-memory.spec.ts</files>
  <action>
Add the Phase 11 database layer for remembered items.

Requirements:
- Use a household-scoped source separate from raw `item_history`
- Normalize item names with one consistent rule such as `lower(trim(name))`
- Store enough information to satisfy all three requirements:
  - user-facing display name
  - latest known category id
  - last used timestamp
  - use count
- Expose a search function or RPC that:
  - starts matching from one typed letter
  - caps results at five
  - narrows as the query becomes more specific
  - ranks by match quality, then household frequency, then recency
- Add focused seed helpers and tests that prove the search contract and most-recent-category behavior are possible to verify downstream

Do not wire UI in this plan.
  </action>
  <verify>
    <automated>npx playwright test tests/item-memory.spec.ts --workers=1</automated>
  </verify>
  <done>Supabase exposes a remembered-item search contract with household isolation, normalization, ranking, and category memory, and test helpers can seed it reliably.</done>
</task>

<task type="auto">
  <name>Task 2: Add a reusable app-side remembered-item query layer and mutation seam</name>
  <files>src/lib/queries/remembered-items.ts, src/lib/queries/items.ts</files>
  <action>
Create the app query surface that the list page can consume in later plans.

Requirements:
- Add a focused query helper around the remembered-item search contract
- Keep search logic out of `ItemInput.svelte`
- Define the mutation/helper seam that will let typed and suggestion-based adds refresh remembered-item memory through the existing list-item flow
- Keep the contract compatible with the current default-quantity rule from Phase 10
- Do not yet build the visible dropdown or change the list page orchestration

Prefer small, explicit types for remembered suggestion rows rather than reusing unrelated recommendation/history types.
  </action>
  <verify>
    <automated>npx playwright test tests/item-memory.spec.ts --workers=1</automated>
  </verify>
  <done>The app has a dedicated remembered-item query layer and a clear mutation seam for future UI integration without duplicating ranking logic in components.</done>
</task>

</tasks>

<verification>
1. `npx playwright test tests/item-memory.spec.ts --workers=1`
2. Supabase migration applies cleanly and the remembered search function returns ranked household-scoped results in local verification.
</verification>

<success_criteria>
- A dedicated remembered-item source exists and stores last category plus ranking metadata
- The app has a reusable query helper for remembered-item search
- Downstream UI work can consume remembered suggestions without scanning raw household history in the browser
</success_criteria>
