---
phase: 11-household-item-memory-and-suggestions
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/lib/components/items/ItemInput.svelte
  - src/routes/(protected)/lister/[id]/+page.svelte
  - src/lib/queries/remembered-items.ts
  - tests/item-memory.spec.ts
  - tests/mobile-layout.spec.ts
autonomous: true
requirements:
  - SUGG-01
  - SUGG-02

must_haves:
  truths:
    - "Typing into the main add field shows household-specific suggestions directly under the field from the first typed letter onward"
    - "The suggestion list narrows as the query becomes more specific and never exceeds five visible rows"
    - "The inline dropdown fits inside the fixed mobile add-bar shell without reintroducing horizontal overflow or layout drift"
    - "Selecting a suggestion behaves like a fast-add action, not a two-step form fill"
  artifacts:
    - path: "src/lib/components/items/ItemInput.svelte"
      provides: "Inline mobile suggestion dropdown anchored to the fixed add bar"
      contains: "suggest"
    - path: "src/routes/(protected)/lister/[id]/+page.svelte"
      provides: "List-page orchestration for live query text, remembered suggestion results, and immediate-add selection"
    - path: "tests/item-memory.spec.ts"
      provides: "Focused recurring-item add-path coverage"
    - path: "tests/mobile-layout.spec.ts"
      provides: "Narrow-phone validation that the dropdown remains contained"
  key_links:
    - from: "src/lib/components/items/ItemInput.svelte"
      to: "src/routes/(protected)/lister/[id]/+page.svelte"
      via: "query text and remembered suggestion selection callbacks"
      pattern: "onSelect"
    - from: "src/routes/(protected)/lister/[id]/+page.svelte"
      to: "src/lib/queries/remembered-items.ts"
      via: "live remembered-item lookups gated by typed input"
      pattern: "create"
---

<objective>
Build the mobile-friendly remembered-suggestion UI into the main add bar and connect it to the list page so recurring items can be added in one tap.

Purpose: satisfy SUGG-01 and SUGG-02 in the real add flow. The feature must feel faster than the existing manual add path while preserving the fixed mobile shell introduced in Phases 9 and 10.

Output: The add field shows a compact household-specific dropdown, the list narrows as the query grows, and tapping a suggestion adds the item immediately.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-household-item-memory-and-suggestions/11-CONTEXT.md
@.planning/phases/11-household-item-memory-and-suggestions/11-RESEARCH.md
@.planning/phases/11-household-item-memory-and-suggestions/11-VALIDATION.md
@.planning/phases/11-household-item-memory-and-suggestions/11-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread remembered search state through the list page and add flow</name>
  <files>src/routes/(protected)/lister/[id]/+page.svelte, src/lib/queries/remembered-items.ts, tests/item-memory.spec.ts</files>
  <action>
Wire remembered-item lookup into the list page without moving mutation ownership into `ItemInput`.

Requirements:
- Track the current add-field query centrally enough to run remembered-item lookup
- Gate lookups so they start from one typed letter and do not refetch unnecessarily
- Feed up to five remembered suggestions into the input component
- Add a separate immediate-add selection path for remembered items rather than pretending they are plain text submits
- Preserve the existing plain typed add path for items with no remembered match
- Add Playwright coverage proving the suggestion list appears and narrows as the query becomes more specific
  </action>
  <verify>
    <automated>npx playwright test tests/item-memory.spec.ts --workers=1</automated>
  </verify>
  <done>The list page can drive remembered search and immediate-add selection while keeping plain typed add intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the inline dropdown UI inside the fixed mobile add bar</name>
  <files>src/lib/components/items/ItemInput.svelte, tests/item-memory.spec.ts, tests/mobile-layout.spec.ts</files>
  <action>
Extend the add bar so remembered suggestions render as an inline dropdown beneath the text field.

Requirements:
- Suggestions appear inside the same max-width add-bar shell
- The dropdown stays compact, scrolls internally if needed, and caps at five visible results
- The visible quantity stepper and add/scan actions from Phase 10 remain usable
- The dropdown must not introduce horizontal overflow on phone-sized viewports
- Selecting a suggestion should trigger the immediate-add callback and then close the dropdown
- Add or update focused Playwright coverage for:
  - first-letter appearance
  - narrowing behavior
  - mobile no-overflow behavior

Prefer stable test IDs or role-based selectors over brittle text-only selectors.
  </action>
  <verify>
    <automated>npx playwright test tests/item-memory.spec.ts tests/mobile-layout.spec.ts --workers=1</automated>
  </verify>
  <done>The fixed add bar shows a contained, tappable remembered-suggestion dropdown that works on narrow mobile screens.</done>
</task>

</tasks>

<verification>
1. `npx playwright test tests/item-memory.spec.ts --workers=1`
2. `npx playwright test tests/item-memory.spec.ts tests/mobile-layout.spec.ts --workers=1`
3. Manual phone check: type one to three letters with the keyboard open and confirm the dropdown stays visible and tappable.
</verification>

<success_criteria>
- Household-specific suggestions appear under the add field from one typed letter
- The suggestion list narrows as more letters are entered
- The dropdown remains mobile-safe inside the fixed add-bar shell
- Suggestion selection triggers immediate add rather than a separate confirm step
</success_criteria>
