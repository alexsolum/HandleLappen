---
phase: 11-household-item-memory-and-suggestions
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/lib/queries/items.ts
  - src/routes/(protected)/lister/[id]/+page.svelte
  - src/lib/components/items/CategoryPickerModal.svelte
  - tests/item-memory.spec.ts
  - tests/helpers/remembered-items.ts
  - tests/mobile-layout.spec.ts
autonomous: true
requirements:
  - SUGG-03
  - SUGG-01
  - SUGG-02

must_haves:
  truths:
    - "Picking a remembered suggestion reuses the most recent valid category for that item name automatically"
    - "Remembered adds bypass the category picker in the normal recurring-item case"
    - "If remembered category data is missing or stale, the app falls back to the existing category-picker flow instead of failing"
    - "Phase 11 closes with end-to-end evidence for suggestion visibility, narrowing, immediate add, and category reuse"
  artifacts:
    - path: "src/lib/queries/items.ts"
      provides: "Shared add-path support for remembered-category assignment and memory refresh"
    - path: "src/routes/(protected)/lister/[id]/+page.svelte"
      provides: "Remembered-add orchestration that bypasses or reopens the category picker correctly"
    - path: "tests/item-memory.spec.ts"
      provides: "End-to-end UI proof for remembered add and category-group placement"
    - path: "tests/helpers/remembered-items.ts"
      provides: "Seeded recurrence/category-memory scenarios"
  key_links:
    - from: "src/routes/(protected)/lister/[id]/+page.svelte"
      to: "src/lib/queries/items.ts"
      via: "remembered suggestion add path with automatic category reuse"
      pattern: "assignCategory"
    - from: "remembered-item source"
      to: "CategoryPickerModal fallback"
      via: "missing-or-invalid category detection"
      pattern: "null"
---

<objective>
Finish the remembered-item flow by restoring the most recent category automatically on suggestion selection, handling stale-memory fallback safely, and closing the phase with end-to-end evidence.

Purpose: satisfy SUGG-03 and verify the full recurring-item behavior. The app should feel fast for the common case while still degrading gracefully when remembered category data is missing or no longer valid.

Output: remembered suggestions add directly into the correct category group when possible, fall back to the existing picker when necessary, and have focused evidence across the Phase 11 surface.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-household-item-memory-and-suggestions/11-CONTEXT.md
@.planning/phases/11-household-item-memory-and-suggestions/11-RESEARCH.md
@.planning/phases/11-household-item-memory-and-suggestions/11-VALIDATION.md
@.planning/phases/11-household-item-memory-and-suggestions/11-01-PLAN.md
@.planning/phases/11-household-item-memory-and-suggestions/11-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reuse remembered categories automatically and preserve fallback safety</name>
  <files>src/lib/queries/items.ts, src/routes/(protected)/lister/[id]/+page.svelte, src/lib/components/items/CategoryPickerModal.svelte, tests/item-memory.spec.ts, tests/helpers/remembered-items.ts</files>
  <action>
Finish the recurring-item add path so suggestion selection restores the most recent valid category automatically.

Requirements:
- Selecting a remembered suggestion should add the item with quantity `1`
- The remembered category should be applied automatically when it still exists in the household
- The normal category picker should not open in the common remembered case
- If the remembered category is missing, deleted, or unresolved, fall back to the existing picker after add
- Refresh remembered-item memory when category changes occur so future suggestion picks stay aligned to the latest category
- Add focused Playwright coverage for:
  - remembered category bypassing the picker
  - most-recent-category winning
  - stale remembered category falling back cleanly
  </action>
  <verify>
    <automated>npx playwright test tests/item-memory.spec.ts --workers=1</automated>
  </verify>
  <done>Remembered suggestions restore the latest valid category automatically and degrade to the existing picker only when the stored category is unusable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close Phase 11 with focused recurring-item verification</name>
  <files>tests/item-memory.spec.ts, tests/mobile-layout.spec.ts</files>
  <action>
Finalize Phase 11 evidence with a focused recurring-item suite.

Coverage requirements:
- first-letter suggestion appearance
- narrowing as more letters are typed
- immediate add on suggestion tap
- default quantity `1` on remembered adds
- remembered category reuse into the correct list group
- stale-category fallback to the picker
- narrow-phone containment for the dropdown

If selectors or seed helpers need refinement, make those changes here rather than spreading brittle test logic into unrelated files.
  </action>
  <verify>
    <automated>npx playwright test tests/item-memory.spec.ts tests/mobile-layout.spec.ts --workers=1</automated>
  </verify>
  <done>Phase 11 has a focused automated verification suite for recurring-item memory, narrowing, immediate add, and category reuse on desktop and mobile-sized layouts.</done>
</task>

</tasks>

<verification>
1. `npx playwright test tests/item-memory.spec.ts --workers=1`
2. `npx playwright test tests/item-memory.spec.ts tests/mobile-layout.spec.ts --workers=1`
3. Manual recurrence check on a phone: add a new categorized item, then start typing it again and confirm the suggestion appears and reuses the expected category.
</verification>

<success_criteria>
- Remembered suggestion selection restores the latest valid category automatically
- Category picker stays out of the way for the normal recurring-item case
- Stale category memory falls back cleanly instead of breaking the add flow
- Phase 11 ends with focused evidence for SUGG-01, SUGG-02, and SUGG-03
</success_criteria>
