---
phase: 04-barcode-scanning
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/04-barcode-scanning/04-VERIFICATION.md
autonomous: true
requirements: [BARC-01, BARC-02, BARC-03, BARC-04]
gap_closure: true

must_haves:
  truths:
    - "BARC-01 through BARC-04 each have a formal definition (checkbox, ID, description) in REQUIREMENTS.md under a v1.0 Barcode Scanning section"
    - "BARC-01 through BARC-04 each have a row in the REQUIREMENTS.md traceability table mapping them to Phase 4 with status Complete"
    - "04-VERIFICATION.md frontmatter status changes from gaps_found to passed"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "v1.0 Barcode Scanning section with four BARC requirement definitions and four traceability rows"
      contains: "BARC-01"
    - path: ".planning/phases/04-barcode-scanning/04-VERIFICATION.md"
      provides: "Updated verification status reflecting gap closure"
      contains: "status: passed"
  key_links:
    - from: "REQUIREMENTS.md traceability table"
      to: "Phase 4"
      via: "BARC-01..04 rows with status Complete"
      pattern: "BARC-0[1-4].*Phase 4.*Complete"
---

<objective>
Close the single administrative gap blocking Phase 4's clean verification pass: BARC-01 through BARC-04 are referenced in ROADMAP.md and in all three Phase 4 plan frontmatter blocks but were never added to REQUIREMENTS.md.

Purpose: Phase 4 holds an 11/12 must-haves score solely because four requirement IDs have no formal definition or traceability record. All functional code is verified. This plan writes the four missing definitions and four missing traceability rows, then updates the verification status to passed.
Output: REQUIREMENTS.md with a v1.0 Barcode Scanning section; 04-VERIFICATION.md with status: passed.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-barcode-scanning/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BARC-01..04 definitions and traceability rows to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read REQUIREMENTS.md in full first (required before any write).

The file currently has no v1.0 section. Insert a new "## v1.0 Requirements" section at the TOP of the file, immediately after the header block (before "## v1.1 Requirements"). The section should contain a "### Barcode Scanning (BARC)" subsection with the four requirement definitions below — all checked [x] because they are complete.

Insert this block:

```
## v1.0 Requirements (Complete)

### Barcode Scanning (BARC)

- [x] **BARC-01**: User taps a "Scan" button, the camera opens, and a detected barcode triggers a product lookup without any additional user action
- [x] **BARC-02**: Kassal.app is the primary provider; when Kassal does not find the EAN, Open Food Facts is used as a silent fallback — the user sees one result or a clear "not found" message, never two separate provider results
- [x] **BARC-03**: Scanned product name and canonical category are normalized via Gemini and pre-filled in the confirmation sheet within 2 seconds for a recognized Norwegian product EAN
- [x] **BARC-04**: Barcode scanning works in iOS Safari PWA standalone mode using the html5-qrcode WASM polyfill; no native BarcodeDetector API is required, and the Kassal.app Bearer token never appears in browser DevTools network requests

```

Then in the "## Traceability" section, add four rows for BARC-01..04 before the existing MOBL-01 row (keeping the table alphabetical by phase number, which means BARC rows come before MOBL rows because Phase 4 precedes Phase 9):

```
| BARC-01 | Phase 4 | Complete |
| BARC-02 | Phase 4 | Complete |
| BARC-03 | Phase 4 | Complete |
| BARC-04 | Phase 4 | Complete |
```

Also update the Coverage summary block at the bottom of the Traceability section:
- Change "v1.2 requirements: 18 total" — leave as-is (v1.2 count is unchanged)
- Add a new line: "v1.0 requirements: 4 total" (placed before the v1.1 line — or add a dedicated v1.0 line)
- Update the last-updated date to today (2026-03-14)

Do NOT alter any other requirement definitions, traceability rows, checkboxes, or out-of-scope entries.
  </action>
  <verify>
    <automated>grep -n "BARC-01\|BARC-02\|BARC-03\|BARC-04" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
grep output shows exactly 8 matches: four from the definition block (the checkbox lines) and four from the traceability table rows, all in .planning/REQUIREMENTS.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update 04-VERIFICATION.md status to passed</name>
  <files>.planning/phases/04-barcode-scanning/04-VERIFICATION.md</files>
  <action>
Read 04-VERIFICATION.md in full first (required before any write).

Make three targeted changes to the YAML frontmatter:

1. Change `status: gaps_found` to `status: passed`
2. Change `score: 11/12 must-haves verified` to `score: 12/12 must-haves verified`
3. Remove the entire `gaps:` block (lines that describe the BARC REQUIREMENTS.md gap) from the frontmatter

In the body of the document, make these changes:

1. In the "## Goal Achievement" table, change the row for truth #12 from:
   `| 12 | BARC-01 through BARC-04 are formally traceable in REQUIREMENTS.md | FAILED | No BARC-* IDs exist anywhere in \`.planning/REQUIREMENTS.md\`...`
   to:
   `| 12 | BARC-01 through BARC-04 are formally traceable in REQUIREMENTS.md | VERIFIED | BARC-01..04 definitions and traceability rows added to \`.planning/REQUIREMENTS.md\` under v1.0 Barcode Scanning section. See Plan 04-04. |`

2. Change the score line `**Score:** 11/12 truths verified` to `**Score:** 12/12 truths verified`

3. Replace the final "## Gaps Summary" section with:

```
## Gaps Summary

All functional code for Phase 4 is implemented, substantive, and wired. Every artifact across all three plans exists with real implementation. All nine key links are verified. The E2E test suite covers all required scenarios with no test.skip stubs.

The administrative gap — BARC-01 through BARC-04 missing from REQUIREMENTS.md — was closed by Plan 04-04 (gap closure). BARC-01..04 definitions and traceability rows now exist in .planning/REQUIREMENTS.md under the v1.0 Barcode Scanning section.

**Phase 4 verdict: PASSED — 12/12 must-haves verified.**
```

4. Update the status line near the top of the document body (after the frontmatter) from:
   `**Status:** gaps_found — one administrative gap (REQUIREMENTS.md traceability) blocks a clean pass; all functional code is verified`
   to:
   `**Status:** passed — all 12 must-haves verified; REQUIREMENTS.md traceability gap closed by Plan 04-04`

Do NOT alter any of the artifact tables, key link tables, requirements coverage table, or anti-patterns section.
  </action>
  <verify>
    <automated>grep -n "status:\|score:\|verdict" .planning/phases/04-barcode-scanning/04-VERIFICATION.md | head -20</automated>
  </verify>
  <done>
grep shows "status: passed" in the frontmatter, "12/12 must-haves verified" in the score line, and "PASSED" in the Gaps Summary section verdict line.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. REQUIREMENTS.md contains BARC-01..04 definitions under a v1.0 Barcode Scanning section
2. REQUIREMENTS.md traceability table has four rows mapping BARC-01..04 to Phase 4 with status Complete
3. 04-VERIFICATION.md frontmatter has status: passed and score: 12/12
4. 04-VERIFICATION.md Gaps Summary section ends with a PASSED verdict

Run: `grep -c "BARC-0" .planning/REQUIREMENTS.md` — expect 8 (four definitions, four traceability rows).
Run: `grep "status:" .planning/phases/04-barcode-scanning/04-VERIFICATION.md | head -1` — expect "status: passed".
</verification>

<success_criteria>
- REQUIREMENTS.md has a v1.0 Requirements section with BARC-01..04 defined and checked [x]
- REQUIREMENTS.md traceability table has BARC-01..04 rows pointing to Phase 4 Complete
- 04-VERIFICATION.md reports status: passed with 12/12 score
- No functional code files are touched — this plan modifies planning documents only
</success_criteria>

<output>
After completion, create `.planning/phases/04-barcode-scanning/04-04-SUMMARY.md` following the standard summary template.
</output>
