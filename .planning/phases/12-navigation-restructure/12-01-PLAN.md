---
phase: 12-navigation-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/navigation.spec.ts
autonomous: true
requirements:
  - NAV-01
  - NAV-02
must_haves:
  truths:
    - "tests/navigation.spec.ts exists and all tests are marked as failing (red) before implementation"
    - "Test file covers: four tabs visible by label, active tab highlights on sub-routes, /oppskrifter and /admin load without error, /husstand redirects to /admin/husstand, /butikker redirects to /admin/butikker"
  artifacts:
    - path: "tests/navigation.spec.ts"
      provides: "Playwright e2e test suite covering NAV-01 and NAV-02"
      exports: []
  key_links:
    - from: "tests/navigation.spec.ts"
      to: "tests/helpers/auth.ts"
      via: "import createHouseholdUser, deleteTestUser"
      pattern: "createHouseholdUser|deleteTestUser"
---

<objective>
Create the Wave 0 test scaffold for Phase 12 navigation changes.

Purpose: Nyquist compliance requires the test file to exist before implementation tasks so the automated verify command in Plans 02 and 03 is not a MISSING reference. Tests start red — they define the contract that implementation must satisfy.
Output: tests/navigation.spec.ts with full coverage of NAV-01 and NAV-02 behaviors.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-navigation-restructure/12-CONTEXT.md
@.planning/phases/12-navigation-restructure/12-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write navigation Playwright test scaffold</name>
  <files>tests/navigation.spec.ts</files>
  <behavior>
    - Test 1 (NAV-01): Four tab labels visible — bottom dock contains exactly the text "Handleliste", "Oppskrifter", "Anbefalinger", "Admin"
    - Test 2 (NAV-01): Active tab on root — navigating to "/" shows Handleliste tab with aria-current="page"
    - Test 3 (NAV-01): Active tab on sub-route — navigating to "/admin/husstand" shows Admin tab with aria-current="page" (not Handleliste)
    - Test 4 (NAV-01): Active tab on /oppskrifter — Oppskrifter tab has aria-current="page"
    - Test 5 (NAV-01): /oppskrifter stub loads without error (200, heading "Oppskrifter" visible)
    - Test 6 (NAV-01): /admin stub loads without error (200, heading "Admin" visible)
    - Test 7 (NAV-02): /husstand redirects to /admin/husstand — final URL after navigation is /admin/husstand
    - Test 8 (NAV-02): /butikker redirects to /admin/butikker — final URL after navigation is /admin/butikker
  </behavior>
  <action>
    Create tests/navigation.spec.ts using Playwright. Reuse the existing test helpers from tests/helpers/auth.ts (createHouseholdUser, deleteTestUser) for authenticated test sessions — match the pattern used in existing spec files (e.g., tests/mobile-layout.spec.ts).

    Structure:
    - beforeAll: createHouseholdUser, sign in, store storageState
    - afterAll: deleteTestUser
    - Use storageState to skip login in each test
    - For redirect tests: call page.goto('/husstand'), then assert page.url() ends with '/admin/husstand' (Playwright follows redirects automatically)
    - For active-tab tests: navigate to the route, then query [data-testid="bottom-dock"] for the anchor with aria-current="page" and assert its text content

    Tests MUST fail at this point because BottomNav still has old tabs (Lister, Husstand, Butikker, Anbefalinger) and the new routes do not exist yet. That is the expected red state.

    Do NOT add --skip-missing or conditional logic to make tests pass early.
  </action>
  <verify>
    <automated>npx playwright test tests/navigation.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <done>tests/navigation.spec.ts exists; running the test command shows failures (not "no tests found" or import errors) — the tests are discovered and fail because the implementation does not match yet</done>
</task>

</tasks>

<verification>
Run: `npx playwright test tests/navigation.spec.ts 2>&1 | tail -20`
Expected: test failures (red) — this is correct for Wave 0. All 8 tests discovered, none passing yet.
</verification>

<success_criteria>
- tests/navigation.spec.ts exists with at least 8 test cases covering both NAV-01 and NAV-02
- Tests use the existing auth helper pattern from tests/helpers/auth.ts
- npx playwright test tests/navigation.spec.ts runs without "no tests found" or syntax errors
- Tests fail (not error) — red state confirms the scaffold is wired correctly
</success_criteria>

<output>
After completion, create `.planning/phases/12-navigation-restructure/12-01-SUMMARY.md`
</output>
