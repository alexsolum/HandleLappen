---
phase: 12-navigation-restructure
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - src/routes/(protected)/husstand/+page.server.ts
  - src/routes/(protected)/butikker/+page.server.ts
autonomous: false
requirements:
  - NAV-02
must_haves:
  truths:
    - "Navigating to /husstand results in a 301 permanent redirect landing at /admin/husstand"
    - "Navigating to /butikker results in a 301 permanent redirect landing at /admin/butikker"
    - "Redirect destinations /admin/husstand and /admin/butikker load without error (stubs created in Plan 02)"
    - "All 8 navigation tests pass green"
  artifacts:
    - path: "src/routes/(protected)/husstand/+page.server.ts"
      provides: "301 permanent redirect from /husstand to /admin/husstand"
      contains: "redirect(301"
    - path: "src/routes/(protected)/butikker/+page.server.ts"
      provides: "301 permanent redirect from /butikker to /admin/butikker"
      contains: "redirect(301"
  key_links:
    - from: "src/routes/(protected)/husstand/+page.server.ts"
      to: "src/routes/(protected)/admin/husstand/+page.svelte"
      via: "throw redirect(301, '/admin/husstand')"
      pattern: "redirect.*admin/husstand"
    - from: "src/routes/(protected)/butikker/+page.server.ts"
      to: "src/routes/(protected)/admin/butikker/+page.svelte"
      via: "throw redirect(301, '/admin/butikker')"
      pattern: "redirect.*admin/butikker"
---

<objective>
Add 301 permanent redirects for the removed top-level routes /husstand and /butikker, then visually verify the complete Phase 12 navigation restructure.

Purpose: Existing PWA users have /husstand and /butikker in their browser back-history and bookmarks. Without redirects these URLs become 404s after the tab removal. Server-side 301 redirects fire before any render, correctly update PWA history, and ensure no user hits a dead end.
Output: Two +page.server.ts files that redirect old routes to their new Admin subpage destinations. Human sign-off on the complete nav change.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-navigation-restructure/12-CONTEXT.md
@.planning/phases/12-navigation-restructure/12-RESEARCH.md
@.planning/phases/12-navigation-restructure/12-02-SUMMARY.md

<interfaces>
<!-- Existing husstand/+page.server.ts currently has a full data-loading load function -->
<!-- It MUST be replaced entirely with the redirect below — do not keep any of the old load logic -->

<!-- Existing redirect pattern in codebase (from auth guards in layout.server.ts): -->
<!-- throw redirect(303, '/logg-inn') -->
<!-- Use throw redirect(301, ...) to stay consistent with codebase style and signal permanence -->

<!-- stub destinations created in Plan 02: -->
<!-- src/routes/(protected)/admin/husstand/+page.svelte — exists -->
<!-- src/routes/(protected)/admin/butikker/+page.svelte — exists -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 301 redirects for /husstand and /butikker</name>
  <files>
    src/routes/(protected)/husstand/+page.server.ts,
    src/routes/(protected)/butikker/+page.server.ts
  </files>
  <action>
    1. REPLACE src/routes/(protected)/husstand/+page.server.ts entirely.
       The current file has a full data-loading load function (reads household data). Remove all of it. The new file is:
       ```typescript
       import { redirect } from '@sveltejs/kit'
       import type { PageServerLoad } from './$types'

       export const load: PageServerLoad = async () => {
         throw redirect(301, '/admin/husstand')
       }
       ```

    2. CREATE src/routes/(protected)/butikker/+page.server.ts (this file does NOT exist yet):
       ```typescript
       import { redirect } from '@sveltejs/kit'
       import type { PageServerLoad } from './$types'

       export const load: PageServerLoad = async () => {
         throw redirect(301, '/admin/butikker')
       }
       ```

    Both redirects use status 301 (permanent) — not 303 — because these are permanent route moves for PWA back-history and bookmark correctness.

    The existing src/routes/(protected)/husstand/+page.svelte can be left in place — the server-side redirect fires before SvelteKit renders the page, so the .svelte file is never reached. Do not delete it.

    Do NOT add await parent() or any data fetching — these redirect immediately with no data needed.
  </action>
  <verify>
    <automated>npx playwright test tests/navigation.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <done>npx playwright test tests/navigation.spec.ts shows all 8 tests passing (green); /husstand and /butikker redirect to their /admin/* destinations; no 404 errors on either redirect target</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Visual verification: complete Phase 12 navigation restructure</name>
  <files></files>
  <action>Human verifies the visual appearance and end-to-end behavior of the new navigation.</action>
  <what-built>Complete Phase 12 navigation restructure: new BottomNav tabs with prefix-based active detection, stub pages for /oppskrifter and /admin, and 301 redirects from /husstand and /butikker</what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Open the app in a browser
    3. Confirm the bottom navigation shows four tabs in this order: Handleliste, Oppskrifter, Anbefalinger, Admin
    4. Tap "Admin" — verify the Admin tab is highlighted and the /admin page shows a list of 5 greyed-out rows (Butikker, Husstand, Historikk, Items, Brukerinnstillinger) with right-facing chevrons
    5. Tap "Oppskrifter" — verify the Oppskrifter tab is highlighted and the page shows "Oppskrifter" heading and "Kommer snart." text
    6. While on /admin, confirm the Admin tab is highlighted (not Handleliste)
    7. Navigate to /admin/husstand directly — confirm the Admin tab is highlighted (sub-route active detection works)
    8. Navigate to /husstand directly — confirm you land on /admin/husstand without a 404
    9. Navigate to /butikker directly — confirm you land on /admin/butikker without a 404
    10. Check the Oppskrifter (book) and Admin (gear) tab icons look visually correct
  </how-to-verify>
  <verify>
    <automated>MISSING — visual icon verification requires human eyes; all other behaviors covered by npx playwright test tests/navigation.spec.ts</automated>
  </verify>
  <done>Human types "approved" or describes issues found</done>
  <resume-signal>Type "approved" to complete Phase 12, or describe any visual/functional issues found</resume-signal>
</task>

</tasks>

<verification>
Run: `npx playwright test tests/navigation.spec.ts`
Expected: All 8 tests green.

Run: `npm run test`
Expected: Full test suite green (mobile-layout.spec.ts must still pass — bottom dock structure unchanged).

Run: `npm run build`
Expected: Build exits 0 with no TypeScript errors.
</verification>

<success_criteria>
- All 8 navigation tests pass
- Full test suite (npm run test) is green
- /husstand redirects to /admin/husstand (browser URL changes, no 404)
- /butikker redirects to /admin/butikker (browser URL changes, no 404)
- Human approves visual appearance of new tabs and Admin hub skeleton
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/12-navigation-restructure/12-03-SUMMARY.md`
</output>
