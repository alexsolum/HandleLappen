# Phase 13-04: Historikk Subpage & Cleanup

## Goal
Extract history into its own page under `/admin/historikk` and clean up `/anbefalinger`.

## Plan
1. Create `src/routes/(protected)/admin/historikk/` directory.
2. Create `src/routes/(protected)/admin/historikk/+page.server.ts`:
   - Fetch `historyGroups` using `createHistoryQuery(locals.supabase)`.
   - Return `historyGroups`.
3. Create `src/routes/(protected)/admin/historikk/+page.svelte`:
   - Extract history UI and logic from `src/routes/(protected)/anbefalinger/+page.svelte`.
   - Add `← Admin` breadcrumb above the `h1`.
4. Update `src/routes/(protected)/anbefalinger/+page.svelte`:
   - Remove history section.
   - Remove `historyGroups` related logic.
5. Update `src/routes/(protected)/anbefalinger/+page.server.ts`:
   - Remove `createHistoryQuery` call.
   - Return only `recommendations`.

## Testing Strategy
- Manual verification: Check `/admin/historikk` for history data and the breadcrumb.
- Manual verification: Check `/anbefalinger` to ensure only recommendations are shown.
- Playwright test: Verify history moving in `tests/admin.spec.ts`.
