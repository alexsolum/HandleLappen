# Phase 13-03: Butikker Subpage Port

## Goal
Port content from `/butikker` to `/admin/butikker` and add the breadcrumb.

## Plan
1. Update `src/routes/(protected)/admin/butikker/+page.svelte`:
   - Copy content from `src/routes/(protected)/butikker/+page.svelte`.
   - Add `← Admin` breadcrumb above the `h1`.
2. Remove `src/routes/(protected)/butikker/+page.svelte`.

## Testing Strategy
- Manual verification: Check `/admin/butikker` for the same store list and the breadcrumb.
- Playwright test: Verify store list and breadcrumb in `tests/admin.spec.ts`.
