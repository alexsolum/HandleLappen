# Phase 13-01: Admin Hub Activation

## Goal
Update `src/routes/(protected)/admin/+page.svelte` to turn stubs into real navigation links for Butikker, Husstand, and Historikk.

## Plan
1. Update `src/routes/(protected)/admin/+page.svelte`:
   - Change `div` to `a` for "Butikker", "Husstand", and "Historikk".
   - Set `href` attributes to `/admin/butikker`, `/admin/husstand`, and `/admin/historikk`.
   - Remove `opacity-50` from active rows.
   - Keep "Items" and "Brukerinnstillinger" as `div` with `opacity-50`.

## Testing Strategy
- Manual verification: Check `/admin` and ensure the 3 rows are clickable and lead to the (currently stubbed) subpages.
- Playwright test: Add a test case in a new `tests/admin.spec.ts` to verify the hub activation.
