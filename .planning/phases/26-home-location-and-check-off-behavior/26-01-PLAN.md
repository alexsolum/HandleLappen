---
phase: 26-home-location-and-check-off-behavior
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260330000000_phase26_home_locations.sql
  - src/lib/types/database.ts
  - src/routes/(protected)/admin/+page.svelte
  - src/routes/(protected)/admin/brukerinnstillinger/+page.server.ts
  - src/routes/(protected)/admin/brukerinnstillinger/+page.svelte
  - tests/admin.spec.ts
  - tests/helpers/location.ts
  - tests/home-location.spec.ts
  - tests/home-location-privacy.spec.ts
autonomous: true
requirements:
  - CHKOFF-03
must_haves:
  truths:
    - "User can open /admin/brukerinnstillinger from the Admin hub and manage home location on that page."
    - "Home location is saved only after an explicit save action, even when Bruk min posisjon is used."
    - "Another household member cannot read the current user's home coordinates through shared profile queries."
  artifacts:
    - path: "supabase/migrations/20260330000000_phase26_home_locations.sql"
      provides: "Private per-user home-location table and RLS policies"
      contains: "create table public.user_home_locations"
    - path: "src/routes/(protected)/admin/brukerinnstillinger/+page.server.ts"
      provides: "Page-scoped load for the current user's home location"
      exports: ["load"]
    - path: "src/routes/(protected)/admin/brukerinnstillinger/+page.svelte"
      provides: "Settings UI with map, current-position shortcut, save/remove controls, and privacy copy"
    - path: "tests/home-location-privacy.spec.ts"
      provides: "Cross-user privacy coverage for home-location storage"
  key_links:
    - from: "src/routes/(protected)/admin/+page.svelte"
      to: "/admin/brukerinnstillinger"
      via: "Admin hub link"
      pattern: "/admin/brukerinnstillinger"
    - from: "src/routes/(protected)/admin/brukerinnstillinger/+page.server.ts"
      to: "public.user_home_locations"
      via: "page-scoped select by auth user id"
      pattern: "from\\('user_home_locations'\\)"
    - from: "src/routes/(protected)/admin/brukerinnstillinger/+page.svelte"
      to: "public.user_home_locations"
      via: "explicit save/remove actions persist and delete only the current user's home-location row"
      pattern: "from\\('user_home_locations'\\)\\.(upsert|insert|delete)"
    - from: "src/routes/(protected)/admin/brukerinnstillinger/+page.svelte"
      to: "src/lib/components/stores/StoreMapWidget.svelte"
      via: "map pin placement"
      pattern: "StoreMapWidget"
---

<objective>
Establish the privacy-safe home-location contract and the real user settings surface needed for Phase 26.

Purpose: Deliver CHKOFF-03 without widening home-coordinate exposure through household-readable `profiles`.
Output: Private home-location schema + RLS, real `/admin/brukerinnstillinger` route, admin link activation, and Wave 0 privacy/settings tests.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-home-location-and-check-off-behavior/26-CONTEXT.md
@.planning/phases/26-home-location-and-check-off-behavior/26-RESEARCH.md
@.planning/phases/26-home-location-and-check-off-behavior/26-VALIDATION.md
@.planning/phases/23-store-location-foundation/23-02-SUMMARY.md
@.planning/phases/24-location-detection-foundation/24-02-SUMMARY.md
@supabase/migrations/20260308000001_phase1_foundation.sql
@src/routes/(protected)/admin/+page.svelte
@src/routes/(protected)/+layout.server.ts
@src/routes/(protected)/admin/husstand/+page.server.ts
@src/lib/components/stores/StoreMapWidget.svelte
@src/lib/location/geolocation.ts
@src/lib/types/database.ts

<interfaces>
From `src/lib/location/geolocation.ts`:

```ts
export async function getCurrentLocation(
  overrides: Partial<PositionOptions> = {}
): Promise<LocationSample>
```

From `src/lib/components/stores/StoreMapWidget.svelte`:

```ts
interface Props {
  lat: number | null
  lng: number | null
  onLocationChange: (lat: number, lng: number) => void
}
```

From `src/routes/(protected)/+layout.server.ts`:

```ts
return { user, householdId: profile.household_id }
```

Preserve the household query discipline from `src/routes/(protected)/admin/husstand/+page.server.ts`:

```ts
.from('profiles')
.select('id, display_name, avatar_url')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 26 Wave 0 settings/privacy test scaffolds</name>
  <read_first>
    .planning/phases/26-home-location-and-check-off-behavior/26-CONTEXT.md
    .planning/phases/26-home-location-and-check-off-behavior/26-RESEARCH.md
    .planning/phases/26-home-location-and-check-off-behavior/26-VALIDATION.md
    tests/admin.spec.ts
    tests/helpers/location.ts
    tests/location-detection.spec.ts
    tests/shopping-mode.spec.ts
  </read_first>
  <files>tests/admin.spec.ts, tests/helpers/location.ts, tests/home-location.spec.ts, tests/home-location-privacy.spec.ts</files>
  <acceptance_criteria>
    - `rg -n "/admin/brukerinnstillinger|Brukerinnstillinger" tests/admin.spec.ts` finds a real navigation assertion instead of the disabled-stub expectation.
    - `rg -n "describe\\(|test\\(" tests/home-location.spec.ts` finds save, remove, and current-position coverage for the settings page.
    - `rg -n "user_home_locations|cannot read|privacy" tests/home-location-privacy.spec.ts` finds explicit privacy assertions for cross-user access.
    - `rg -n "seedHomeLocation|mockCurrentHomePosition|setHomeLocation" tests/helpers/location.ts` finds helper APIs for Phase 26 tests.
  </acceptance_criteria>
  <action>Create the missing Phase 26 test files before production work so verification is executable from the start. Replace the current admin-hub stub test with real `/admin/brukerinnstillinger` navigation coverage. Extend `tests/helpers/location.ts` with private-home-location seeding helpers and a current-position mock helper that mirrors the existing geolocation mocking style. Add `tests/home-location.spec.ts` covering map-pin save, `Bruk min posisjon` populating pending coordinates without persisting, and remove-home flow. Add `tests/home-location-privacy.spec.ts` proving the second household member cannot read another user's saved home location through browser-visible queries or direct table access. Keep the helpers and selectors aligned with the existing Phase 23/24/25 Playwright patterns.</action>
  <verify>
    <automated>npx playwright test tests/admin.spec.ts --project=chromium</automated>
  </verify>
  <done>Wave 0 files exist with real test bodies, the admin test targets a real settings link, and the new Phase 26 tests fail only because the app behavior is not implemented yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add private home-location schema and page-scoped server contract</name>
  <read_first>
    .planning/phases/26-home-location-and-check-off-behavior/26-CONTEXT.md
    .planning/phases/26-home-location-and-check-off-behavior/26-RESEARCH.md
    supabase/migrations/20260308000001_phase1_foundation.sql
    src/routes/(protected)/+layout.server.ts
    src/routes/(protected)/admin/husstand/+page.server.ts
    src/lib/types/database.ts
    tests/home-location-privacy.spec.ts
  </read_first>
  <files>supabase/migrations/20260330000000_phase26_home_locations.sql, src/lib/types/database.ts, src/routes/(protected)/admin/brukerinnstillinger/+page.server.ts</files>
  <acceptance_criteria>
    - `rg -n "create table public.user_home_locations|enable row level security|auth.uid\\(\\)" supabase/migrations/20260330000000_phase26_home_locations.sql` finds a dedicated table with own-row RLS policies.
    - `rg -n "lat_4dp|lng_4dp|round\\(" supabase/migrations/20260330000000_phase26_home_locations.sql` finds 4-decimal precision enforcement in the database contract.
    - `rg -n "user_home_locations" src/lib/types/database.ts` finds generated types for the new table.
    - `rg -n "from\\('user_home_locations'\\)|safeGetSession|maybeSingle" "src/routes/(protected)/admin/brukerinnstillinger/+page.server.ts"` finds a page-scoped load limited to the current user.
    - `rg -n "home_lat|home_lng" src/routes/(protected)/admin/husstand/+page.server.ts src/routes/(protected)/+layout.server.ts` returns no matches.
  </acceptance_criteria>
  <action>Implement the privacy-safe storage model recommended by research: create `public.user_home_locations` keyed by `user_id`, with `lat_4dp` and `lng_4dp` numeric values rounded to 4 decimals, `created_at`, and `updated_at`, plus own-row select/insert/update/delete policies using `auth.uid()`. Do not add `home_lat` or `home_lng` to `profiles`; the plan is explicitly using a private table because `profiles_select` is household-readable. Regenerate or update `src/lib/types/database.ts` so the new table is available to route code. Add `src/routes/(protected)/admin/brukerinnstillinger/+page.server.ts` that loads only the signed-in user's row with `safeGetSession()`, returns `homeLocation: null | { lat: number; lng: number }`, and never broadens `(protected)` layout data or household member queries.</action>
  <verify>
    <automated>npx playwright test tests/home-location-privacy.spec.ts --project=chromium --grep "privacy|RLS|cannot read"</automated>
  </verify>
  <done>The database exposes a private own-row home-location table with 4-decimal precision, route-level server load can fetch only the current user's saved home point, and no shared profile query path gained home-coordinate fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the focused user settings page and activate the admin entry point</name>
  <read_first>
    .planning/phases/26-home-location-and-check-off-behavior/26-CONTEXT.md
    .planning/phases/26-home-location-and-check-off-behavior/26-RESEARCH.md
    src/routes/(protected)/admin/+page.svelte
    src/routes/(protected)/admin/brukerinnstillinger/+page.server.ts
    src/lib/components/stores/StoreMapWidget.svelte
    src/lib/location/geolocation.ts
    tests/admin.spec.ts
    tests/home-location.spec.ts
  </read_first>
  <files>src/routes/(protected)/admin/+page.svelte, src/routes/(protected)/admin/brukerinnstillinger/+page.svelte</files>
  <acceptance_criteria>
    - `rg -n "/admin/brukerinnstillinger" "src/routes/(protected)/admin/+page.svelte"` finds the admin hub link.
    - `rg -n "Bruk min posisjon|Fjern hjemmeposisjon|Lagre hjemmeposisjon" "src/routes/(protected)/admin/brukerinnstillinger/+page.svelte"` finds the required controls on one page.
    - `rg -n "StoreMapWidget|getCurrentLocation|aria-live" "src/routes/(protected)/admin/brukerinnstillinger/+page.svelte"` finds map pin reuse, current-position shortcut wiring, and inline feedback.
    - `rg -n "ikke delt|kun din konto|rydde opp" "src/routes/(protected)/admin/brukerinnstillinger/+page.svelte"` finds always-visible privacy copy matching the locked disclosure.
  </acceptance_criteria>
  <action>Turn the disabled Admin hub row into a real `/admin/brukerinnstillinger` navigation target. Build a minimal mobile-first settings page focused only on home location: show a single card with Norwegian page title, always-visible privacy explanation, `StoreMapWidget` reused for pin placement, a `Bruk min posisjon` action that calls `getCurrentLocation()` and only updates pending map state, an explicit save button that persists the rounded coordinates to `user_home_locations`, and a `Fjern hjemmeposisjon` control that deletes the row from the same screen. Keep the map and save/remove controls on the same page, keep the pending-vs-saved distinction explicit, and do not auto-save when current position is used.</action>
  <verify>
    <automated>npx playwright test tests/home-location.spec.ts --project=chromium --grep "save|remove|current position"</automated>
  </verify>
  <done>Users can reach `/admin/brukerinnstillinger` from Admin, place or update a home pin with explicit save semantics, use current position without auto-persisting, remove the saved location on the same screen, and see fixed inline privacy copy.</done>
</task>

</tasks>

<verification>
Run `npx playwright test tests/admin.spec.ts tests/home-location.spec.ts tests/home-location-privacy.spec.ts --project=chromium` after the plan completes. Confirm the database migration leaves `profiles` untouched for home coordinates and that page-scoped loading is the only read path.
</verification>

<success_criteria>
CHKOFF-03 is ready when the app exposes a real settings page from Admin, home coordinates are stored privately per user with 4-decimal precision, and privacy tests prove household peers cannot read them.
</success_criteria>

<output>
After completion, create `.planning/phases/26-home-location-and-check-off-behavior/26-home-location-and-check-off-behavior-01-SUMMARY.md`
</output>
