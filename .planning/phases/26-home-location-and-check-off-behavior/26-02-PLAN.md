---
phase: 26-home-location-and-check-off-behavior
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - src/lib/location/proximity.ts
  - src/lib/offline/queue.ts
  - src/lib/queries/items.ts
  - src/routes/(protected)/lister/[id]/+page.svelte
  - tests/helpers/location.ts
  - tests/home-location.spec.ts
  - tests/offline.spec.ts
autonomous: true
requirements:
  - CHKOFF-02
must_haves:
  truths:
    - "Checking off an item near home removes it from the list without creating shopping history."
    - "Shopping mode still wins over home cleanup when both contexts could apply."
    - "At-home cleanup behaves correctly through offline queue replay instead of silently reverting to history insertion."
  artifacts:
    - path: "src/lib/queries/items.ts"
      provides: "Home-cleanup mutation branching before history insertion"
      exports: ["createCheckOffMutation"]
    - path: "src/lib/offline/queue.ts"
      provides: "Offline queue semantics that distinguish home-delete from history-toggle"
    - path: "tests/offline.spec.ts"
      provides: "Regression coverage for offline home cleanup replay"
  key_links:
    - from: "src/routes/(protected)/lister/[id]/+page.svelte"
      to: "src/lib/queries/items.ts"
      via: "checkOffMutation inputs including home context and toast handling"
      pattern: "createCheckOffMutation"
    - from: "src/lib/queries/items.ts"
      to: "src/lib/offline/queue.ts"
      via: "enqueue replay mode for home delete vs history toggle"
      pattern: "enqueue\\("
    - from: "src/lib/queries/items.ts"
      to: "src/lib/location/proximity.ts"
      via: "100m near-home distance check"
      pattern: "haversineDistanceMeters|HOME_DETECTION_RADIUS_METERS"
---

<objective>
Finish Phase 26 by making list check-offs honor the saved home location online and offline without breaking shopping-mode behavior.

Purpose: Deliver CHKOFF-02 cleanly and keep recommendations/history free from at-home cleanup noise.
Output: 100m home-detection branching, shopping-mode precedence, subtle toast UX, offline queue contract updates, and end-to-end regression coverage.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-home-location-and-check-off-behavior/26-CONTEXT.md
@.planning/phases/26-home-location-and-check-off-behavior/26-RESEARCH.md
@.planning/phases/26-home-location-and-check-off-behavior/26-VALIDATION.md
@.planning/phases/25-shopping-mode/25-02-SUMMARY.md
@src/lib/location/session.svelte.ts
@src/lib/location/proximity.ts
@src/lib/offline/queue.ts
@src/lib/queries/items.ts
@src/routes/(protected)/lister/[id]/+page.svelte
@tests/shopping-mode.spec.ts
@tests/offline.spec.ts
@tests/home-location.spec.ts

<interfaces>
From `src/lib/location/session.svelte.ts`:

```ts
locationSession.shoppingModeActive: boolean
locationSession.lastSample: LocationSample | null
locationSession.detectedStoreId: string | null
```

From `src/lib/location/proximity.ts`:

```ts
export function haversineDistanceMeters(
  from: { latitude: number; longitude: number },
  to: { latitude: number; longitude: number }
): number
```

From `src/lib/queries/items.ts`:

```ts
type ToggleItemVariables = {
  itemId: string
  isChecked: boolean
  itemName: string
  historyContext?: {
    listName?: string | null
    storeId?: string | null
    storeName?: string | null
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Branch list check-off behavior for near-home cleanup with shopping-mode precedence</name>
  <read_first>
    .planning/phases/26-home-location-and-check-off-behavior/26-CONTEXT.md
    .planning/phases/26-home-location-and-check-off-behavior/26-RESEARCH.md
    src/lib/location/session.svelte.ts
    src/lib/location/proximity.ts
    src/lib/queries/items.ts
    src/routes/(protected)/lister/[id]/+page.svelte
    tests/home-location.spec.ts
    tests/shopping-mode.spec.ts
  </read_first>
  <files>src/lib/location/proximity.ts, src/lib/queries/items.ts, src/routes/(protected)/lister/[id]/+page.svelte, tests/home-location.spec.ts</files>
  <acceptance_criteria>
    - `rg -n "HOME_DETECTION_RADIUS_METERS = 100" src/lib/location/proximity.ts` finds the locked near-home threshold.
    - `rg -n "shoppingModeActive" src/lib/queries/items.ts src/routes/(protected)/lister/[id]/+page.svelte` finds explicit shopping-mode precedence in the branching inputs.
    - `rg -n "delete\\(\\)\\.eq\\('id', itemId\\)|mode: 'home-delete'|atHomeCleanup" src/lib/queries/items.ts` finds delete-before-history logic.
    - `rg -n "aria-live|rydde opp hjemme|hjemme" "src/routes/(protected)/lister/[id]/+page.svelte"` finds the subtle at-home cleanup toast on the list page.
    - `rg -n "does not create history|shopping mode wins|toast" tests/home-location.spec.ts` finds the matching e2e assertions.
  </acceptance_criteria>
  <action>Add a dedicated `HOME_DETECTION_RADIUS_METERS = 100` helper path in `src/lib/location/proximity.ts` rather than reusing the 150m store geofence constant. Extend `createCheckOffMutation` so checked items branch in this order: shopping mode active -> existing history path; otherwise if a saved home location exists and `locationSession.lastSample` is within 100m -> delete the `list_items` row immediately and skip `item_history`; otherwise keep the normal toggle behavior. Pass the current user's saved home point and latest location sample from the list page into the mutation without moving location logic into a new global engine. Add a subtle `aria-live="polite"` toast on the list page explaining that the item was removed as at-home cleanup. Do not add a confirmation dialog, and do not let near-home cleanup override active shopping mode.</action>
  <verify>
    <automated>npx playwright test tests/home-location.spec.ts tests/shopping-mode.spec.ts --project=chromium</automated>
  </verify>
  <done>Online check-offs near home delete the item instead of creating history, the list explains the disappearance with a subtle toast, and active shopping mode still routes check-offs through the store-history path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the offline queue contract so at-home cleanup replays correctly</name>
  <read_first>
    .planning/phases/26-home-location-and-check-off-behavior/26-RESEARCH.md
    src/lib/offline/queue.ts
    src/lib/queries/items.ts
    tests/offline.spec.ts
    tests/home-location.spec.ts
  </read_first>
  <files>src/lib/offline/queue.ts, src/lib/queries/items.ts, tests/offline.spec.ts</files>
  <acceptance_criteria>
    - `rg -n "type: 'toggle'|'home-delete'|mode" src/lib/offline/queue.ts` finds distinct queue semantics beyond the current single `toggle` type.
    - `rg -n "replayMutation|delete\\(\\)|item_history" src/lib/offline/queue.ts` finds replay logic that deletes for home cleanup and inserts history only for history-capable toggles.
    - `rg -n "offline.*home|replay.*home|does not create history" tests/offline.spec.ts` finds explicit offline replay coverage for at-home cleanup.
    - `rg -n "enqueue\\(" src/lib/queries/items.ts` finds queue payloads carrying the chosen replay mode.
  </acceptance_criteria>
  <action>Close the research-identified offline gap instead of scoping it away. Evolve `QueuedMutation` so replay can distinguish a home cleanup delete from the legacy history-writing toggle path. When a user checks off near home while offline, enqueue a `home-delete` style entry containing the item and list identifiers plus timestamp metadata needed for deterministic replay. Update `replayMutation` and `replayBatch` so home-delete entries remove the row without inserting `item_history`, while shopping/manual history entries continue to use the existing toggle-plus-history flow. Preserve the Phase 21 survivor behavior: successful home-delete replays must be cleared even if later entries fail.</action>
  <verify>
    <automated>npx playwright test tests/offline.spec.ts tests/home-location.spec.ts --project=chromium</automated>
  </verify>
  <done>Offline at-home cleanup survives reconnect without creating history rows, queue replay remains deterministic, and mixed replay outcomes still keep earlier successes cleared.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize Phase 26 regression coverage across settings, privacy, list, and offline flows</name>
  <read_first>
    .planning/phases/26-home-location-and-check-off-behavior/26-VALIDATION.md
    tests/admin.spec.ts
    tests/home-location.spec.ts
    tests/home-location-privacy.spec.ts
    tests/offline.spec.ts
    tests/shopping-mode.spec.ts
  </read_first>
  <files>tests/helpers/location.ts, tests/home-location.spec.ts, tests/offline.spec.ts</files>
  <acceptance_criteria>
    - `npx playwright test tests/admin.spec.ts tests/home-location.spec.ts tests/home-location-privacy.spec.ts tests/offline.spec.ts tests/shopping-mode.spec.ts --project=chromium` passes.
    - `rg -n "100m|shopping mode|offline replay|privacy" tests/home-location.spec.ts tests/home-location-privacy.spec.ts tests/offline.spec.ts` finds explicit assertions for each Phase 26 risk area.
  </acceptance_criteria>
  <action>Complete the test matrix instead of leaving split coverage assumptions. Tighten helper setup so the same utilities can seed a saved home point, drive the list page with near-home coordinates, and verify that at-home cleanup produces no history row both online and after offline replay. Keep the existing shopping-mode coverage running in the same wave to prove that the new home branch does not regress shopping-mode precedence.</action>
  <verify>
    <automated>npx playwright test tests/admin.spec.ts tests/home-location.spec.ts tests/home-location-privacy.spec.ts tests/offline.spec.ts tests/shopping-mode.spec.ts --project=chromium</automated>
  </verify>
  <done>Phase 26 ends with passing automated coverage for settings navigation, privacy boundaries, online at-home cleanup, offline at-home replay, and shopping-mode precedence.</done>
</task>

</tasks>

<verification>
Run `npx playwright test tests/admin.spec.ts tests/home-location.spec.ts tests/home-location-privacy.spec.ts tests/offline.spec.ts tests/shopping-mode.spec.ts --project=chromium` and confirm that near-home cleanup never inserts `item_history` unless shopping mode is active.
</verification>

<success_criteria>
CHKOFF-02 is complete when at-home check-offs delete list items without polluting history, shopping mode still attributes real shopping check-offs, and the offline queue replays the same behavior correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/26-home-location-and-check-off-behavior/26-home-location-and-check-off-behavior-02-SUMMARY.md`
</output>
