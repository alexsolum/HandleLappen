---
phase: 18-ios-scanner-black-screen-fix
plan: 02
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified: []
autonomous: false
requirements:
  - SCAN-01
  - SCAN-02
  - SCAN-03
must_haves:
  truths:
    - "Camera viewfinder is visible within 2 seconds of tapping scan button on an iPhone installed as a PWA"
    - "Denying camera access in iOS Settings shows Settings-guidance text with no retry button"
    - "Tapping outside the permission prompt shows Prøv igjen retry button"
    - "Scanning a barcode produces a haptic pulse on an Android device or desktop Chrome"
  artifacts:
    - path: "src/lib/barcode/scanner.ts"
      provides: "MutationObserver intercept confirmed deployed"
    - path: "src/lib/components/barcode/BarcodeScannerSheet.svelte"
      provides: "Split error states and haptic call confirmed in production build"
  key_links:
    - from: "Physical iPhone (PWA installed)"
      to: "Live viewfinder visible within 2s"
      via: "MutationObserver intercept setting playsinline before stream attaches"
      pattern: "manual verification"
---

<objective>
Verify that the iOS scanner black screen fix, permission UX split, and haptic feedback work correctly on real devices.

Purpose: Playwright tests cannot reproduce the iOS Safari PWA black screen (WebKit bug 252465 only manifests in standalone mode on a physical device). This checkpoint provides the human-verified gate for SCAN-01.
Output: Confirmed pass or list of issues for gap closure.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-ios-scanner-black-screen-fix/18-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify SCAN-01/02/03 on real devices</name>
  <action>Human verifies scanner fixes on physical iPhone (PWA mode) and Android/Chrome for haptic. No automation — simulator cannot reproduce the iOS Safari WebKit black screen bug (252465).</action>
  <what-built>
    Plan 18-01 implemented three scanner fixes:
    - MutationObserver intercept sets playsinline/muted/autoplay on the video element before html5-qrcode start() resolves (SCAN-01)
    - Camera permission denied vs dismissed mapped to two separate UX states: Settings guidance (permanent block) or Prøv igjen retry (dismissed) (SCAN-02)
    - navigator.vibrate(50) fires on barcode detection before lookup starts, guarded for unsupported browsers (SCAN-03)
  </what-built>
  <how-to-verify>
    Before verifying, run dev server: `npm run dev` (or deploy to staging)

    **SCAN-01 — iOS black screen fix (requires physical iPhone installed as PWA):**
    1. Open the app in Mobile Safari on your iPhone
    2. Tap the Share icon → "Add to Home Screen" → add it
    3. Open the app from the home screen (must be in standalone/PWA mode — no Safari browser chrome)
    4. Navigate to a shopping list
    5. Tap "Skann strekkode"
    6. Expected: the live camera viewfinder appears within 2 seconds — no black screen
    7. Also test: tap scan button, see viewfinder, close, tap again — viewfinder appears on second open too

    **SCAN-02 — Permission UX (test in browser or iOS):**
    Denied path (requires camera to be blocked in Settings):
    8. On iPhone: Settings → Safari → Camera → Deny (or use a browser where camera is blocked)
    9. Open the scanner
    10. Expected: text contains "Innstillinger" and "Kamera" (Settings guidance), NO "Prøv igjen" button visible

    Dismissed path (tap outside the permission prompt):
    11. On a device/browser where camera is in "prompt" state (not yet decided)
    12. Open the scanner, when the camera permission prompt appears, tap Cancel or outside it
    13. Expected: "Prøv igjen" button visible, no Settings-guidance text

    **SCAN-03 — Haptic feedback (Android Chrome or desktop Chrome):**
    14. Open the app on an Android device or use Chrome on desktop
    15. Navigate to a shopping list, tap "Skann strekkode"
    16. Scan a real barcode (or use browser DevTools to check navigator.vibrate is called)
    17. Expected: brief haptic pulse felt on Android; no error on iOS (silent degradation)

    Note: If you only have an iPhone available, steps 14-17 can be verified by opening Chrome DevTools
    Console and running: `navigator.vibrate(50)` — if it returns `true`, the API is supported and
    the guarded call will work. The vibrate spy test from Plan 18-01 also confirms correct behavior.
  </how-to-verify>
  <verify>
    <automated>MISSING — SCAN-01 black screen fix requires physical iPhone in installed PWA mode; simulator cannot reproduce WebKit bug 252465</automated>
  </verify>
  <done>User confirms viewfinder visible within 2s on iPhone PWA (SCAN-01), correct UX for both denied and dismissed states (SCAN-02), and haptic pulse works on Android/Chrome (SCAN-03)</done>
  <resume-signal>
    Type "approved" if SCAN-01/02/03 all pass on device.
    Or describe any issues found (e.g., "black screen still appears on iOS 18.2") so gap closure can be planned.
  </resume-signal>
</task>

</tasks>

<verification>
Human confirmation that:
1. Camera viewfinder visible within 2 seconds on iPhone PWA (SCAN-01)
2. Denied state shows Settings guidance only, no retry button (SCAN-02)
3. Dismissed state shows Prøv igjen only, no Settings guidance (SCAN-02)
4. Haptic confirmed on Android/Chrome or vibrate spy test passed (SCAN-03)
</verification>

<success_criteria>
- User types "approved" confirming all three requirements pass on device
- Phase 18 can be marked complete
- SCAN-01, SCAN-02, SCAN-03 checkboxes updated to checked in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/18-ios-scanner-black-screen-fix/18-02-SUMMARY.md`
</output>
