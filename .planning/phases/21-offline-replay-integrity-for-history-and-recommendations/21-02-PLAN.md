---
phase: 21-offline-replay-integrity-for-history-and-recommendations
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - tests/recommendations.spec.ts
  - tests/helpers/history.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Recommendation inputs stay stable after mixed replay failure and retry; successful history writes are not counted twice."
    - "A deterministic recommendation regression test fails if replay reintroduces duplicate history counts from the same logical check-off."
    - "Phase 21 verification includes a direct automated check anchored in recommendation flow tests."
  artifacts:
    - path: "tests/recommendations.spec.ts"
      provides: "Regression test for recommendation stability after success/failure replay and retry cycles."
    - path: "tests/helpers/history.ts"
      provides: "Household/list-scoped history count helper(s) used by recommendation assertions."
  key_links:
    - from: "tests/recommendations.spec.ts"
      to: "tests/helpers/history.ts"
      via: "test validates recommendation-source history counts before and after replay retry"
      pattern: "countHistoryRows"
    - from: "tests/recommendations.spec.ts"
      to: "src/routes/(protected)/anbefalinger/+page.server.ts"
      via: "navigates to /anbefalinger with active list context to validate recommendation flow remains usable"
      pattern: "goto\(`/anbefalinger\?list="
---

<objective>
Prove recommendation integrity after replay retries by adding an end-to-end regression that would fail if mixed replay outcomes duplicate source history rows.

Purpose: Close audit scope for recommendation skew by validating replay correctness in the recommendation flow, not only queue internals.
Output: Stable recommendation regression test plus helper-level assertions on recommendation source counts.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-offline-replay-integrity-for-history-and-recommendations/21-RESEARCH.md
@.planning/phases/21-offline-replay-integrity-for-history-and-recommendations/21-01-PLAN.md
@tests/recommendations.spec.ts
@tests/helpers/history.ts

<interfaces>
From tests/recommendations.spec.ts:
```typescript
test.describe('recommendations', () => { ... })
```

From tests/helpers/history.ts:
```typescript
export async function createHistorySessions(listId: string, sessions: HistorySessionSeed[]): Promise<void>
export async function clearHistoryForList(listId: string): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend recommendation test helpers with explicit source-count assertions</name>
  <files>tests/helpers/history.ts</files>
  <read_first>
    - tests/helpers/history.ts
    - tests/recommendations.spec.ts
  </read_first>
  <behavior>
    - Test 1: helper returns deterministic integer count for item_history rows by list and normalized item name.
    - Test 2: helper throws on Supabase errors instead of returning silent defaults.
  </behavior>
  <action>Add helper in `tests/helpers/history.ts`:
1. `export async function countHistoryRowsByListAndItem(listId: string, itemName: string): Promise<number>`.
2. Query shape must be exactly:
   - `from('item_history').select('id', { count: 'exact', head: true }).eq('list_id', listId).ilike('item_name', itemName)`
3. Return `count ?? 0`.
4. Throw query error directly when present.
5. Keep existing helper exports intact and backward compatible.</action>
  <acceptance_criteria>
    - `rg -n "export async function countHistoryRowsByListAndItem" tests/helpers/history.ts` returns one match.
    - `rg -n "select\('id', \{ count: 'exact', head: true \}\)" tests/helpers/history.ts` returns one match in the new helper.
    - `rg -n "throw error" tests/helpers/history.ts` returns a match within the new helper block.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/recommendations.spec.ts --grep "cold-start" --reporter=list</automated>
  </verify>
  <done>Recommendation specs can assert exact `item_history` source counts without ad-hoc query logic in test bodies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add replay-retry recommendation stability regression</name>
  <files>tests/recommendations.spec.ts, tests/helpers/history.ts</files>
  <read_first>
    - tests/recommendations.spec.ts
    - tests/helpers/history.ts
    - tests/offline.spec.ts
    - src/routes/(protected)/+layout.svelte
  </read_first>
  <behavior>
    - Test 1: replay with success then forced failure then retry yields stable source counts (no double count of already-successful item).
    - Test 2: after retry, recommendation page still renders recommendation list for active list context.
  </behavior>
  <action>Add a new test in `tests/recommendations.spec.ts` named exactly `stable after replay retry does not inflate recommendation source counts` with concrete flow:
1. Seed 10 sessions where `Melk` appears in each session and at least one paired item (`Bananer`) appears to keep recommendations active.
2. Create live list rows for `Melk` and `Brød` via `createTestItem`.
3. Navigate to `/lister/${fixture!.listId}`, set browser offline, check off `Melk` and `Brød` so two queue entries exist.
4. Install a one-time route interceptor on `**/rest/v1/item_history*` that returns HTTP 500 only when payload targets `Brød`, allowing `Melk` to succeed.
5. Reconnect once, assert `countHistoryRowsByListAndItem(fixture!.listId, 'Melk')` increased by exactly 1 and `Brød` remains unchanged.
6. Remove interceptor, force second reconnect cycle, assert final counts increased by exactly one for `Brød` and still only one increment for `Melk`.
7. Navigate to `/anbefalinger?list=${fixture!.listId}` and assert `data-testid="recommendation-list"` is visible and contains at least one `recommendation-row`.
8. Keep existing recommendation tests unchanged and preserve fixture cleanup.</action>
  <acceptance_criteria>
    - `rg -n "stable after replay retry does not inflate recommendation source counts" tests/recommendations.spec.ts` returns one match.
    - `rg -n "countHistoryRowsByListAndItem" tests/recommendations.spec.ts tests/helpers/history.ts` returns matches in both files.
    - `rg -n "\*\*/rest/v1/item_history\*" tests/recommendations.spec.ts` returns one match.
    - `rg -n "recommendation-list|recommendation-row" tests/recommendations.spec.ts` returns assertions in the new test block.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/recommendations.spec.ts --grep "stable after replay retry|blends co-purchase" --reporter=list</automated>
  </verify>
  <done>Recommendation regression coverage fails on replay-induced duplicate history growth and passes when replay survivor logic is correct.</done>
</task>

</tasks>

<verification>
- Run targeted recommendation regression: `npx playwright test tests/recommendations.spec.ts --grep "stable after replay retry|blends co-purchase" --reporter=list`.
- Run cross-flow regression subset: `npx playwright test tests/offline.spec.ts tests/recommendations.spec.ts --grep "replay|stable" --reporter=list`.
</verification>

<success_criteria>
- Recommendation-source history counts remain stable through mixed replay failure+retry sequences.
- Recommendation UI remains functional after replay retries for an active list.
- Automated tests provide a deterministic guard against replay-driven recommendation skew regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/21-offline-replay-integrity-for-history-and-recommendations/21-02-SUMMARY.md`
</output>
