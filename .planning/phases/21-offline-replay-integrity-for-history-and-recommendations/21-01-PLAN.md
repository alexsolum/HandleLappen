---
phase: 21-offline-replay-integrity-for-history-and-recommendations
plan: 01
type: execute
wave: 1
depends_on:
  - 20-client-image-display
files_modified:
  - src/lib/offline/queue.ts
  - src/routes/(protected)/+layout.svelte
  - tests/offline.spec.ts
  - tests/helpers/history.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "When reconnect replay contains both successes and failures, successful entries are removed from the offline queue in the same drain cycle."
    - "A retry cycle replays only the failed entries from the prior drain, not entries that already succeeded."
    - "History rows for already-successful replayed check-offs are not duplicated across reconnect retries."
  artifacts:
    - path: "src/lib/offline/queue.ts"
      provides: "Deterministic replay batch API that returns succeeded/failed counts and failed survivors to persist."
    - path: "src/routes/(protected)/+layout.svelte"
      provides: "Queue drain orchestration that persists only failed survivors instead of all-or-nothing clear."
    - path: "tests/offline.spec.ts"
      provides: "Mixed replay outcome regression tests (success then failure then retry) with queue and history assertions."
    - path: "tests/helpers/history.ts"
      provides: "Admin test helper to count item_history rows for list/item assertions."
  key_links:
    - from: "src/routes/(protected)/+layout.svelte"
      to: "src/lib/offline/queue.ts"
      via: "drainQueue invokes replay batch API and writes failed survivors back to storage"
      pattern: "replayBatch.*replaceQueue"
    - from: "tests/offline.spec.ts"
      to: "tests/helpers/history.ts"
      via: "mixed replay test verifies no duplicate rows via helper count"
      pattern: "countHistoryRowsForItem"
---

<objective>
Fix the replay integrity root cause by replacing all-or-nothing clearing with partial acknowledgement and prove deterministic mixed-outcome retry behavior.

Purpose: Close the audit gap where successful replayed check-offs are replayed again after a later failure in the same batch.
Output: Deterministic queue replay contract in production code plus offline regression tests that assert survivor-set and history-idempotency behavior.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-offline-replay-integrity-for-history-and-recommendations/21-RESEARCH.md
@src/lib/offline/queue.ts
@src/routes/(protected)/+layout.svelte
@tests/offline.spec.ts
@tests/helpers/history.ts

<interfaces>
From src/lib/offline/queue.ts:
```typescript
export type QueuedMutation = {
  id: string
  type: 'toggle'
  payload: {
    itemId: string
    listId: string
    isChecked: boolean
    itemName: string
    userId: string
    timestamp: string
  }
  enqueuedAt: string
}

export async function getAll(): Promise<QueuedMutation[]>
export async function replayMutation(supabase: SupabaseClient, entry: QueuedMutation): Promise<void>
```

From src/routes/(protected)/+layout.svelte:
```typescript
async function drainQueue(): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic replay-batch contract in offline queue module</name>
  <files>src/lib/offline/queue.ts</files>
  <read_first>
    - src/lib/offline/queue.ts
    - src/lib/stores/offline.svelte.ts
  </read_first>
  <behavior>
    - Test 1: replay batch with one replay failure returns `succeeded = 1`, `failed = 1`, and survivors containing only the failed entry.
    - Test 2: replay batch with all successes returns `succeeded = queued.length`, `failed = 0`, and empty survivors.
  </behavior>
  <action>Add concrete queue APIs in `src/lib/offline/queue.ts`:
1. `export type ReplayBatchResult = { succeeded: number; failed: number; survivors: QueuedMutation[] }`.
2. `export async function replaceQueue(entries: QueuedMutation[]): Promise<void>` that calls the existing internal `writeQueue(entries)`.
3. `export async function replayBatch(supabase: SupabaseClient, queued: QueuedMutation[]): Promise<ReplayBatchResult>` that loops entries in order, calls `replayMutation` per entry, increments `succeeded` on success, pushes failed entries into `survivors`, and returns `{ succeeded, failed: survivors.length, survivors }`.
4. Preserve existing `enqueue`, `getAll`, and `clear` exports unchanged so existing callers remain compatible.
5. Do not introduce new storage keys or dependencies.</action>
  <acceptance_criteria>
    - `rg -n "export type ReplayBatchResult" src/lib/offline/queue.ts` returns one match.
    - `rg -n "export async function replaceQueue" src/lib/offline/queue.ts` returns one match.
    - `rg -n "export async function replayBatch" src/lib/offline/queue.ts` returns one match.
    - `rg -n "survivors" src/lib/offline/queue.ts` returns matches inside `replayBatch` return shape.
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>`queue.ts` exposes a deterministic replay-batch API that explicitly returns failed survivors and success/failure counts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace all-or-nothing drain logic with partial-ack survivor persistence</name>
  <files>src/routes/(protected)/+layout.svelte</files>
  <read_first>
    - src/routes/(protected)/+layout.svelte
    - src/lib/offline/queue.ts
    - src/lib/stores/offline.svelte.ts
  </read_first>
  <behavior>
    - Test 1: mixed replay outcome persists only failed survivors and updates pending count to failed count.
    - Test 2: full replay success persists empty queue and keeps success toast behavior.
  </behavior>
  <action>Update `drainQueue` in `src/routes/(protected)/+layout.svelte` with concrete contract changes:
1. Replace `clear` import with `replaceQueue`, and import `replayBatch` from `$lib/offline/queue`.
2. Remove `let allSucceeded = true` and per-entry try/catch loop.
3. After `const queued = await getAll()`, call `const result = await replayBatch(data.supabase, queued)`.
4. Persist queue state with `await replaceQueue(result.survivors)` on every drain attempt (success or partial failure), then `await refreshPendingCount()`.
5. Keep success toast only when `result.succeeded > 0 && result.failed === 0`.
6. Keep existing `drainingQueue` guard and online/offline guard logic unchanged.
7. Keep existing toast message text `Endringer synkronisert` unchanged.</action>
  <acceptance_criteria>
    - `rg -n "import \{ getAll, replayBatch, replaceQueue \}" src/routes/(protected)/+layout.svelte` returns one match.
    - `rg -n "allSucceeded" src/routes/(protected)/+layout.svelte` returns no matches.
    - `rg -n "await replaceQueue\(result\.survivors\)" src/routes/(protected)/+layout.svelte` returns one match.
    - `rg -n "result\.succeeded > 0 && result\.failed === 0" src/routes/(protected)/+layout.svelte` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Reconnect drain now acknowledges partial success deterministically by retaining only failed entries for retry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add mixed replay regression proving no duplicate history rows on retry</name>
  <files>tests/offline.spec.ts, tests/helpers/history.ts</files>
  <read_first>
    - tests/offline.spec.ts
    - tests/helpers/history.ts
    - tests/helpers/lists.ts
    - src/routes/(protected)/+layout.svelte
    - src/lib/offline/queue.ts
  </read_first>
  <behavior>
    - Test 1: success-then-failure replay leaves queue pending count at 1 and keeps first item's history count at exactly 1.
    - Test 2: retry replays only survivor, resulting counts exactly `itemA=1` and `itemB=1` (no duplicate for itemA).
  </behavior>
  <action>Implement deterministic mixed-outcome E2E regression:
1. In `tests/helpers/history.ts`, add `export async function countHistoryRowsForItem(listId: string, itemName: string): Promise<number>` using admin client query:
   - `from('item_history').select('id', { count: 'exact', head: true }).eq('list_id', listId).eq('item_name', itemName)`
   - return `count ?? 0` and throw when Supabase returns error.
2. In `tests/offline.spec.ts`, import `countHistoryRowsForItem`.
3. Add test named exactly `mixed replay outcome keeps successful entries cleared and avoids duplicate history on retry`.
4. In that test: create a second item (name `Brød`) via `createTestItem(fixture!.listId, 'Brød', 1)` before queueing.
5. Queue two check-offs offline (`Melk` then `Brød`) and assert pending debug count reaches 2.
6. Before first reconnect, install `page.route('**/rest/v1/item_history*', ...)` interceptor that fails exactly one POST where payload `item_name === 'Brød'` with status `500`, and continues all other requests.
7. Reconnect (`context.setOffline(false)`), assert pending count becomes 1, assert `await countHistoryRowsForItem(fixture!.listId, 'Melk') === 1`, assert `await countHistoryRowsForItem(fixture!.listId, 'Brød') === 0`.
8. Remove interception, trigger another offline->online cycle to replay survivor, then assert pending count 0 and final counts `Melk=1`, `Brød=1`.
9. Keep existing tests unchanged in behavior and names.</action>
  <acceptance_criteria>
    - `rg -n "countHistoryRowsForItem" tests/helpers/history.ts tests/offline.spec.ts` returns matches in both files.
    - `rg -n "mixed replay outcome keeps successful entries cleared and avoids duplicate history on retry" tests/offline.spec.ts` returns one match.
    - `rg -n "\*\*/rest/v1/item_history\*" tests/offline.spec.ts` returns one match.
    - `rg -n "Melk\) === 1|Brød\) === 1" tests/offline.spec.ts` returns assertion matches.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/offline.spec.ts --grep "mixed replay outcome|sync toast after reconnect" --reporter=list</automated>
  </verify>
  <done>Offline replay regression coverage proves partial-ack behavior and idempotent history outcome across failure+retry cycles.</done>
</task>

</tasks>

<verification>
- Run targeted replay tests: `npx playwright test tests/offline.spec.ts --grep "mixed replay outcome|sync toast after reconnect" --reporter=list`.
- Run phase regression subset: `npx playwright test tests/offline.spec.ts tests/history.spec.ts tests/recommendations.spec.ts --reporter=list`.
</verification>

<success_criteria>
- Successful replayed entries are removed even when a later replay in the same drain fails.
- Retry drains replay only survivor failures.
- Mixed replay retry cycles do not duplicate previously successful `item_history` rows.
</success_criteria>

<output>
After completion, create `.planning/phases/21-offline-replay-integrity-for-history-and-recommendations/21-01-SUMMARY.md`
</output>
