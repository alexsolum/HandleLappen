---
phase: 06-history-view-and-recommendations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260311000001_phase6_history_snapshots.sql
  - src/lib/types/database.ts
  - src/lib/queries/items.ts
  - src/lib/queries/history.ts
  - src/routes/(protected)/anbefalinger/+page.server.ts
  - src/routes/(protected)/anbefalinger/+page.svelte
  - tests/history.spec.ts
  - tests/helpers/history.ts
autonomous: true
requirements:
  - HIST-02

must_haves:
  truths:
    - "Users can open a protected Phase 6 history surface and see past shopping sessions grouped by date"
    - "Within each date group, sessions are ordered newest first and are collapsed by default"
    - "History rows show compact item names plus who checked each item off"
    - "History session headers can prefer store-first for new rows while older rows without store metadata degrade to list-name-only"
    - "History reads remain household-scoped through RLS-safe queries"
  artifacts:
    - path: "supabase/migrations/20260311000001_phase6_history_snapshots.sql"
      provides: "nullable snapshot fields and any helper structures needed for Phase 6 history display"
    - path: "src/lib/queries/history.ts"
      provides: "history query factories and grouping helpers for Phase 6"
      exports: ["createHistoryQuery"]
    - path: "src/routes/(protected)/anbefalinger/+page.svelte"
      provides: "history UI with date groups and collapsed sessions"
      contains: "Historikk"
    - path: "tests/history.spec.ts"
      provides: "history smoke/integration coverage for grouping and attribution"
  key_links:
    - from: "src/lib/queries/items.ts"
      to: "supabase/migrations/20260311000001_phase6_history_snapshots.sql"
      via: "check-off history write includes new snapshot fields when available"
      pattern: "item_history"
    - from: "src/routes/(protected)/anbefalinger/+page.server.ts"
      to: "src/lib/queries/history.ts"
      via: "route data hydrates history surface"
      pattern: "anbefalinger"
---

<objective>
Create the Phase 6 history foundation: durable history metadata, grouped household history queries, and the initial protected history surface that satisfies HIST-02.

Purpose: recommendations depend on history being queryable and displayable first. The user decisions require compact, date-grouped sessions with store-first headers where possible, but current history rows do not yet capture stable store context.

Output: migration for history snapshot metadata, history query module, protected `/anbefalinger` page rendering compact grouped history, and Wave 0/plan-level Playwright coverage in `tests/history.spec.ts`.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-history-view-and-recommendations/06-CONTEXT.md
@.planning/phases/06-history-view-and-recommendations/06-RESEARCH.md
@.planning/phases/06-history-view-and-recommendations/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — history fixtures and Playwright scaffold</name>
  <files>
    tests/history.spec.ts,
    tests/helpers/history.ts
  </files>
  <action>
Create `tests/helpers/history.ts` with deterministic admin helpers for:
- creating checked-off history rows with `checked_at`, `checked_by`, and optional store snapshot fields
- creating multiple same-day and multi-day session seeds
- cleaning up seeded history records for a test list/household

Create `tests/history.spec.ts` with initial coverage for:
1. grouped-by-date rendering
2. collapsed session rows by default
3. member attribution on item rows
4. legacy rows without store snapshot rendering only list name

Follow existing Playwright helper patterns already used in `tests/helpers/auth.ts` and `tests/helpers/lists.ts`.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/history.spec.ts</automated>
  </verify>
  <done>`tests/history.spec.ts` and `tests/helpers/history.ts` exist with runnable scaffolds tied to HIST-02.</done>
</task>

<task type="auto">
  <name>Task 2: Add durable history snapshot metadata and write-path support</name>
  <files>
    supabase/migrations/20260311000001_phase6_history_snapshots.sql,
    src/lib/types/database.ts,
    src/lib/queries/items.ts
  </files>
  <action>
Add a Phase 6 migration that extends `item_history` with the minimum nullable snapshot fields needed for store-first history headers on future rows. Prefer snapshot fields that do not mutate when store names change later.

Also update the check-off write path in `src/lib/queries/items.ts` so new history entries can populate the new snapshot fields when store context is available, while remaining backwards-compatible when it is not.

Do not break existing HIST-01 behavior or older rows.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`item_history` supports Phase 6 display metadata for new rows, types compile, and current check-off writes remain functional.</done>
</task>

<task type="auto">
  <name>Task 3: Build grouped history query and compact history surface</name>
  <files>
    src/lib/queries/history.ts,
    src/routes/(protected)/anbefalinger/+page.server.ts,
    src/routes/(protected)/anbefalinger/+page.svelte,
    tests/history.spec.ts
  </files>
  <action>
Implement a household-scoped history query module and render the first Phase 6 surface at `/anbefalinger`.

Requirements for the UI:
- top-level date groups
- newest sessions first within each date
- sessions collapsed by default
- store-first headers for rows that have snapshot store metadata
- older rows with no store snapshot show list name only
- compact row treatment with item name + member attribution

The page can be recommendations-empty for now; this plan is responsible for the history section and route shell only.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/history.spec.ts</automated>
  </verify>
  <done>The protected `/anbefalinger` route renders grouped compact history and Playwright verifies the expected HIST-02 behaviors.</done>
</task>

</tasks>

<must_have_self_check>
- History display does not depend on current session-local `selectedStoreId` to render older rows
- The initial `/anbefalinger` route exists and is protected
- `HIST-02` is visibly satisfied by this plan's output, not deferred
</must_have_self_check>
