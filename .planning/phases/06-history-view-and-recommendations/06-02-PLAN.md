---
phase: 06-history-view-and-recommendations
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - supabase/migrations/20260311000002_phase6_recommendations.sql
  - src/lib/types/database.ts
  - src/lib/queries/recommendations.ts
  - src/routes/(protected)/anbefalinger/+page.server.ts
  - src/routes/(protected)/anbefalinger/+page.svelte
  - tests/recommendations.spec.ts
  - tests/helpers/history.ts
autonomous: true
requirements:
  - RECD-01
  - RECD-02

must_haves:
  truths:
    - "The household's top purchased items from the last 90 days are queryable and render as recommendations"
    - "Co-purchase suggestions are derived from real item_history sessions and exclude items already on the target list"
    - "Frequency and co-purchase results are combined into one blended compact list"
    - "Before 10 sessions exist, recommendations are gated by a direct cold-start message"
    - "Cold-start state still leaves the Phase 6 surface useful instead of showing fake placeholders"
  artifacts:
    - path: "supabase/migrations/20260311000002_phase6_recommendations.sql"
      provides: "SQL helpers/views/functions for household frequency and co-purchase recommendations"
    - path: "src/lib/queries/recommendations.ts"
      provides: "recommendations query layer and cold-start gating helpers"
      exports: ["createRecommendationsQuery"]
    - path: "tests/recommendations.spec.ts"
      provides: "recommendation and cold-start Playwright coverage"
  key_links:
    - from: "src/routes/(protected)/anbefalinger/+page.server.ts"
      to: "src/lib/queries/recommendations.ts"
      via: "server load populates recommendations section"
      pattern: "recommend"
    - from: "src/lib/queries/recommendations.ts"
      to: "item_history"
      via: "household frequency and co-purchase SQL"
      pattern: "item_history"
---

<objective>
Implement recommendation data and presentation for the `Anbefalinger` surface: household frequency recommendations, active-list co-purchase suggestions, and the explicit cold-start gate.

Purpose: Phase 6 is recommendation-led. After history exists, the user should see one practical blended list, not separate dashboards or placeholder cards.

Output: recommendation SQL/migration artifacts, query module, blended recommendation section on `/anbefalinger`, and automated coverage for frequency, co-purchase, and cold-start behaviors.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-history-view-and-recommendations/06-CONTEXT.md
@.planning/phases/06-history-view-and-recommendations/06-RESEARCH.md
@.planning/phases/06-history-view-and-recommendations/06-VALIDATION.md
@.planning/phases/06-history-view-and-recommendations/06-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SQL support for frequency and co-purchase recommendations</name>
  <files>
    supabase/migrations/20260311000002_phase6_recommendations.sql,
    src/lib/types/database.ts,
    tests/helpers/history.ts
  </files>
  <action>
Add the SQL layer needed for:
- household top items by count in the last 90 days
- co-purchase items bought in the same shopping session window
- session counting for the cold-start threshold of 10 sessions

Prefer deterministic SQL functions/views that stay within the current Supabase/RLS model. Keep the logic household-scoped and SQL-only; do not introduce ML or service-side recommendation jobs.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Phase 6 recommendation queries exist in SQL-compatible form and generated types compile.</done>
</task>

<task type="auto">
  <name>Task 2: Build recommendation query module and cold-start gate</name>
  <files>
    src/lib/queries/recommendations.ts,
    src/routes/(protected)/anbefalinger/+page.server.ts,
    src/routes/(protected)/anbefalinger/+page.svelte,
    tests/recommendations.spec.ts
  </files>
  <action>
Implement the recommendation query layer and wire it into the existing `/anbefalinger` page.

UI and behavior constraints:
- recommendations-first layout
- one blended compact list for frequency + co-purchase
- explicit prompt to open a list if no active list context exists
- explicit direct message when fewer than 10 sessions exist
- cold-start layout should feel friendly, not dashboard-empty

The recommendation section may be read-only in this plan; add-back is handled in plan 06-03.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/recommendations.spec.ts</automated>
  </verify>
  <done>The recommendations surface shows frequency/co-purchase data when eligible and the cold-start gate when not, with automated coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Refine blended ordering and no-active-list prompt behavior</name>
  <files>
    src/lib/queries/recommendations.ts,
    src/routes/(protected)/anbefalinger/+page.svelte,
    tests/recommendations.spec.ts
  </files>
  <action>
Finalize how the single blended list is ordered and labeled so the UI remains practical rather than looking like two merged feeds. Ensure the no-active-list prompt appears only when needed and does not suppress history utility during cold start.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/recommendations.spec.ts -g "frequency|co-purchase|cold-start"</automated>
  </verify>
  <done>The blended recommendation section is stable, compact, and aligned with the locked Phase 6 context decisions.</done>
</task>

</tasks>

<must_have_self_check>
- `RECD-01` and `RECD-02` are satisfied by actual SQL-backed outputs
- Cold-start directly mentions the 10-session threshold
- The page still provides value when recommendations are gated
</must_have_self_check>
