---
phase: 06-history-view-and-recommendations
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
autonomous: true
gap_closure: true
files_modified:
  - src/lib/queries/items.ts
  - src/routes/(protected)/anbefalinger/+page.svelte
  - tests/history.spec.ts
  - tests/recommendations.spec.ts
requirements:
  - HIST-02
  - RECD-03

must_haves:
  truths:
    - "Add-back from history or recommendations restores items as active shopping items, not as checked/done items"
    - "If a checked copy of the item already exists, add-back creates or updates an unchecked row instead of incrementing the done row"
    - "Chooser fallback and subtle toast behavior remain intact after the fix"
  artifacts:
    - path: "src/lib/queries/items.ts"
      provides: "add-back mutation logic that distinguishes checked vs unchecked existing rows"
    - path: "tests/history.spec.ts"
      provides: "regression coverage for restoring history items into unchecked state"
    - path: "tests/recommendations.spec.ts"
      provides: "regression coverage ensuring direct add-back still lands in active items"
  key_links:
    - from: "src/routes/(protected)/anbefalinger/+page.svelte"
      to: "src/lib/queries/items.ts"
      via: "history and recommendation add-back actions reuse the corrected mutation"
      pattern: "addBack"
---

<objective>
Close the Phase 6 UAT gap where add-back can attach to an already checked item row, causing the restored item to appear in the done section instead of the active shopping list.

Purpose: the Phase 6 browsing loop is only correct if add-back restores actionable unchecked items. Quantity increment should apply to an existing active row, not resurrect a checked row as still checked.

Output: corrected add-back mutation behavior, no regression in chooser/toast flow, and targeted E2E coverage for unchecked restoration.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-history-view-and-recommendations/06-CONTEXT.md
@.planning/phases/06-history-view-and-recommendations/06-RESEARCH.md
@.planning/phases/06-history-view-and-recommendations/06-VALIDATION.md
@.planning/phases/06-history-view-and-recommendations/06-UAT.md
@.planning/phases/06-history-view-and-recommendations/06-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct add-back matching so checked rows do not absorb restores</name>
  <files>
    src/lib/queries/items.ts
  </files>
  <action>
Update the add-back mutation so it does not match a checked `list_item` row as the preferred quantity-increment target.

Required behavior:
- if an unchecked row with the same normalized item name exists, increment that row
- otherwise insert a new unchecked row
- never leave add-back landing in the done section just because a checked row already exists

Keep the fix narrow. Do not change the existing recommendation/history trigger surfaces yet unless the mutation contract requires a small adjustment.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Add-back logic restores active items correctly even when the chosen list already contains a checked copy.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for restoring into unchecked state</name>
  <files>
    tests/history.spec.ts,
    tests/recommendations.spec.ts
  </files>
  <action>
Extend the Phase 6 Playwright coverage with explicit regression assertions for the UAT bug:
- history add-back into a list that already has the same item checked should result in an unchecked active row
- recommendation add-back should still work in the direct-target case and land in active items

Retain chooser and toast expectations so the fix does not silently regress the rest of the Phase 6 add-back contract.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/history.spec.ts tests/recommendations.spec.ts -g "add-back|add back"</automated>
  </verify>
  <done>Targeted E2E coverage fails without the fix and passes with the corrected unchecked-restoration behavior.</done>
</task>

</tasks>

<must_have_self_check>
- Add-back never revives a checked row as still checked
- Quantity increment only targets an existing active row
- The chooser fallback and subtle toast remain unchanged from the approved Phase 6 UX
</must_have_self_check>
