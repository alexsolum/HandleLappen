---
phase: 06-history-view-and-recommendations
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/lib/components/lists/BottomNav.svelte
  - src/lib/stores/active-list.svelte.ts
  - src/lib/components/recommendations/ListTargetSheet.svelte
  - src/lib/queries/items.ts
  - src/routes/(protected)/anbefalinger/+page.svelte
  - src/routes/(protected)/lister/[id]/+page.svelte
  - tests/recommendations.spec.ts
  - tests/history.spec.ts
autonomous: true
requirements:
  - HIST-02
  - RECD-03

must_haves:
  truths:
    - "Bottom navigation exposes a working `Anbefalinger` route instead of a disabled placeholder"
    - "Users can add back an item from history or recommendations in one action when the target list is obvious"
    - "If no obvious list exists, the user is required to choose a list rather than the app guessing"
    - "If the target list already contains the item, quantity is increased instead of creating a duplicate row"
    - "A subtle toast confirms successful add-back"
  artifacts:
    - path: "src/lib/components/lists/BottomNav.svelte"
      provides: "active recommendations tab route"
    - path: "src/lib/stores/active-list.svelte.ts"
      provides: "lightweight current-list targeting context for Phase 6 add-back heuristics"
    - path: "src/lib/components/recommendations/ListTargetSheet.svelte"
      provides: "explicit list chooser when no obvious current list exists"
    - path: "tests/recommendations.spec.ts"
      provides: "add-back and tab activation coverage"
  key_links:
    - from: "src/routes/(protected)/lister/[id]/+page.svelte"
      to: "src/lib/stores/active-list.svelte.ts"
      via: "list page updates current-list context"
      pattern: "active-list"
    - from: "src/routes/(protected)/anbefalinger/+page.svelte"
      to: "src/lib/queries/items.ts"
      via: "history/recommendation tap triggers add or quantity increment"
      pattern: "mutate"
---

<objective>
Finish the Phase 6 user loop by activating the `Anbefalinger` tab and implementing add-back actions from history/recommendations into shopping lists.

Purpose: the phase is not done until users can act on what they see. This plan turns the Phase 6 surface from read-only insight into a practical shopping shortcut.

Output: active bottom-nav route, current-list targeting heuristic + chooser fallback, quantity-increment dedup behavior, subtle success toast, and end-to-end tests for add-back behavior.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-history-view-and-recommendations/06-CONTEXT.md
@.planning/phases/06-history-view-and-recommendations/06-RESEARCH.md
@.planning/phases/06-history-view-and-recommendations/06-VALIDATION.md
@.planning/phases/06-history-view-and-recommendations/06-01-PLAN.md
@.planning/phases/06-history-view-and-recommendations/06-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate the recommendations route in BottomNav and establish active-list context</name>
  <files>
    src/lib/components/lists/BottomNav.svelte,
    src/lib/stores/active-list.svelte.ts,
    src/routes/(protected)/lister/[id]/+page.svelte
  </files>
  <action>
Replace the disabled `Anbefalinger` placeholder with a working protected route link.

Introduce the smallest viable shared current-list context so the app can detect when a list target is genuinely obvious. Keep the heuristic narrow and safe: wrong auto-targeting is worse than opening the chooser.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/recommendations.spec.ts -g "tab"</automated>
  </verify>
  <done>`Anbefalinger` is navigable from BottomNav and the app can distinguish obvious-vs-ambiguous list targets.</done>
</task>

<task type="auto">
  <name>Task 2: Implement add-back actions with quantity increment and chooser fallback</name>
  <files>
    src/lib/components/recommendations/ListTargetSheet.svelte,
    src/lib/queries/items.ts,
    src/routes/(protected)/anbefalinger/+page.svelte
  </files>
  <action>
Wire tap actions from history and recommendation rows to:
- add directly when the target list is obvious
- otherwise open a list chooser sheet
- increment quantity when the item already exists on the target list
- show a subtle toast after success

Reuse the existing list/item mutation model instead of building a parallel write system.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/recommendations.spec.ts tests/history.spec.ts -g "add back"</automated>
  </verify>
  <done>Users can add back items from Phase 6 surfaces with the exact targeting and dedup rules locked in context.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end polish and full Phase 6 surface verification</name>
  <files>
    src/routes/(protected)/anbefalinger/+page.svelte,
    tests/recommendations.spec.ts,
    tests/history.spec.ts
  </files>
  <action>
Finalize the integrated Phase 6 flow:
- active `Anbefalinger` tab
- recommendations-first layout
- cold-start message
- grouped history access
- add-back actions with subtle confirmation

Ensure test seeds cover:
- fewer than 10 sessions
- 10+ sessions
- active-list auto-target case
- ambiguous list chooser case
- existing-item quantity increment case
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/history.spec.ts tests/recommendations.spec.ts</automated>
  </verify>
  <done>Phase 6 has an end-to-end validated user flow from seeing history/recommendations to acting on them.</done>
</task>

</tasks>

<must_have_self_check>
- `RECD-03` is satisfied by a real bottom-nav route and real tab content
- Add-back behavior exactly follows the context decisions on chooser fallback, subtle toast, and quantity increment
- Phase 6 remains compact and practical on mobile rather than becoming a dashboard
</must_have_self_check>
