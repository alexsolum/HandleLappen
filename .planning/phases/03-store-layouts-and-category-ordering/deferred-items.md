- 2026-03-10: `npx playwright test tests/auth.spec.ts` still has three failing login-flow tests (`session persistence @smoke`, `sign out clears session @smoke`, `protected route redirects after logout`). Failures occur before the new category-seeding path is exercised and were left out of scope for plan `03-01`.
- 2026-03-10: `npx tsc --noEmit` generic typing errors in [src/lib/queries/items.ts](/C:/Users/HP/Documents/Koding/HandleAppen/src/lib/queries/items.ts) and [src/lib/queries/lists.ts](/C:/Users/HP/Documents/Koding/HandleAppen/src/lib/queries/lists.ts) were resolved during plan `03-02` to satisfy the compile gate.
- 2026-03-10: `npx playwright test --reporter=list` still fails in pre-existing auth and household flows (`tests/auth.spec.ts`, `tests/household.spec.ts`) plus an existing delete-list timeout in [tests/lists.spec.ts](/C:/Users/HP/Documents/Koding/HandleAppen/tests/lists.spec.ts). The new category grouping tests pass and were left in scope for plan `03-02`.
- 2026-03-10: `npx playwright test` after plan `03-04` still fails in pre-existing auth/login coverage (`session persistence @smoke`, `sign out clears session @smoke`, `protected route redirects after logout`) and the existing `lists delete list` timeout in [tests/lists.spec.ts](/C:/Users/HP/Documents/Koding/HandleAppen/tests/lists.spec.ts). Category assignment, item history, and grouping flows added in `03-04` pass.
