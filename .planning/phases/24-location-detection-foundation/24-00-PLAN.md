---
phase: 24-location-detection-foundation
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers/location.ts
  - tests/location-detection.spec.ts
  - .planning/phases/24-location-detection-foundation/24-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - LOCATE-01
  - LOCATE-02
  - LOCATE-03

must_haves:
  truths:
    - "Phase 24 implementation work starts with requirement-specific validation artifacts already in place"
    - "Each Phase 24 requirement has an exact Playwright title/filter that can be targeted without waiting for the broader 45-second suite"
    - "The installed-iPhone PWA verification path is written down before UI work starts, so manual sign-off is not invented late"
  artifacts:
    - path: "tests/helpers/location.ts"
      provides: "Reusable seeded-store, geolocation-mock, and visibility helpers for Phase 24"
      exports: ["seedLocatedStore", "installGeolocationMock", "setDocumentVisibility"]
    - path: "tests/location-detection.spec.ts"
      provides: "Requirement-aligned Playwright scenarios with stable filter substrings for LOCATE-01, LOCATE-02, and LOCATE-03"
      contains: "manual picker fallback"
    - path: ".planning/phases/24-location-detection-foundation/24-MANUAL-CHECKLIST.md"
      provides: "Installed-iPhone PWA checklist for prompt, deny, retry, unavailable, and background/resume validation"
      contains: "Install the PWA on a physical iPhone"
  key_links:
    - from: "tests/location-detection.spec.ts"
      to: "tests/helpers/location.ts"
      via: "Phase 24 scenarios share seeded store, geolocation mock, and visibility helpers"
      pattern: "installGeolocationMock|seedLocatedStore|setDocumentVisibility"
    - from: "tests/location-detection.spec.ts"
      to: ".planning/phases/24-location-detection-foundation/24-VALIDATION.md"
      via: "Exact stable substrings match the documented validation filters"
      pattern: "permission flow|foreground poller|manual picker fallback"
---

<objective>
Create the Wave 0 validation scaffolding Phase 24 was missing: reusable location-test helpers, requirement-aligned Playwright spec skeletons, and the installed-iPhone manual checklist.

Purpose: Research and validation already require these artifacts before implementation. This plan makes the behavioral suite available from the first code task and introduces a sub-30-second smoke path (`--list` with exact filters) before the broader ~45-second phase run.

Output: `tests/helpers/location.ts`, `tests/location-detection.spec.ts`, and `.planning/phases/24-location-detection-foundation/24-MANUAL-CHECKLIST.md`.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-location-detection-foundation/24-CONTEXT.md
@.planning/phases/24-location-detection-foundation/24-RESEARCH.md
@.planning/phases/24-location-detection-foundation/24-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Phase 24 Playwright helpers and requirement-aligned spec titles</name>
  <files>tests/helpers/location.ts, tests/location-detection.spec.ts</files>
  <read_first>
    tests/helpers/auth.ts,
    tests/helpers/lists.ts,
    tests/full-user-flow.spec.ts,
    .planning/phases/24-location-detection-foundation/24-RESEARCH.md,
    .planning/phases/24-location-detection-foundation/24-VALIDATION.md
  </read_first>
  <action>
Create `tests/helpers/location.ts` with exact helper responsibilities:

- `seedLocatedStore(householdId: string, input: { chain: string | null; locationName: string; lat: number; lng: number })`
  - Insert into `stores` using the current schema fields `chain`, `location_name`, `lat`, `lng`.
  - Also create the required `store_layouts` rows for the household categories, mirroring the pattern in `tests/helpers/categories.ts`.
- `installGeolocationMock(page, mode)`
  - Supported modes: `'nearby-success'`, `'no-nearby-store'`, `'permission-denied'`, `'position-unavailable'`
  - Override `navigator.geolocation.getCurrentPosition` via `page.addInitScript`.
  - Track request count on `window.__HANDLEAPPEN_LOCATION_REQUESTS__`.
- `setDocumentVisibility(page, state: 'hidden' | 'visible')`
  - Override `document.visibilityState` via `Object.defineProperty` and dispatch `visibilitychange`.

Create `tests/location-detection.spec.ts` with these exact test names so the documented validation filters continue to work unchanged:
- `permission flow waits for explicit confirm tap before first geolocation request`
- `foreground poller auto-selects nearby store and resumes after visibility restore`
- `manual picker fallback stays available when location is denied or unavailable`

Test expectations:
- First test: the request counter stays `0` on page load and after the first CTA tap; it becomes `1` only after clicking `Fortsett`.
- Second test: a seeded store near the mocked coordinates is auto-selected; hiding the document stops the counter from increasing; restoring visibility causes one immediate new request and keeps the selected store visible.
- Third test: `Velg butikk manuelt` remains visible before permission, after denied, and after unavailable; inline denied/unavailable copy is visible and the retry button appears.

Keep this plan as scaffolding only. Do not add production-code assertions that require Phase 24 implementation to exist yet beyond the route, copy anchors, and helper contracts above.
  </action>
  <verify>
    <automated>npx playwright test tests/location-detection.spec.ts --list</automated>
  </verify>
  <done>The Phase 24 helper module and Playwright spec exist, load cleanly, and expose exact stable titles containing `permission flow`, `foreground poller`, and `manual picker fallback` for requirement-targeted smoke runs.</done>
</task>

<task type="auto">
  <name>Task 2: Create the installed-iPhone PWA manual checklist before implementation starts</name>
  <files>.planning/phases/24-location-detection-foundation/24-MANUAL-CHECKLIST.md</files>
  <read_first>
    .planning/phases/24-location-detection-foundation/24-CONTEXT.md,
    .planning/phases/24-location-detection-foundation/24-RESEARCH.md,
    .planning/phases/24-location-detection-foundation/24-VALIDATION.md
  </read_first>
  <action>
Create `.planning/phases/24-location-detection-foundation/24-MANUAL-CHECKLIST.md` with numbered installed-iPhone PWA steps covering:
1. Install the PWA on a physical iPhone home screen.
2. Open a real list page and confirm no location prompt appears before interaction.
3. Tap `Slå på automatisk butikkvalg`, confirm the explanation appears, then tap `Fortsett`.
4. Verify the iOS location prompt appears.
5. Deny once, verify inline retry state appears and manual picker is still usable.
6. Retry and verify Settings guidance appears if Safari no longer re-prompts.
7. Re-enable location, background the app for at least 30 seconds, reopen, and confirm an immediate refresh occurs.
8. Turn off Location Services temporarily and verify the unavailable message still leaves manual selection usable.

Add a short note at the end stating that the smallest Phase 24 smoke path is the `--list` title check from this Wave 0 plan, while the broader `npx playwright test tests/location-detection.spec.ts` run remains the wave-level contract from `24-VALIDATION.md`.
  </action>
  <verify>
    <automated>rg -n "Install the PWA on a physical iPhone|Slå på automatisk butikkvalg|Fortsett|manual selection usable|smallest Phase 24 smoke path" .planning/phases/24-location-detection-foundation/24-MANUAL-CHECKLIST.md</automated>
  </verify>
  <done>The manual checklist exists before UI implementation and explicitly documents the installed-iPhone prompt, deny/retry, unavailable, and background/resume checks plus the smallest available smoke path.</done>
</task>

</tasks>

<verification>
- `npx playwright test tests/location-detection.spec.ts -g "permission flow" --list`
- `npx playwright test tests/location-detection.spec.ts -g "foreground poller" --list`
- `npx playwright test tests/location-detection.spec.ts -g "manual picker fallback" --list`
- `npx playwright test tests/location-detection.spec.ts`
</verification>

<success_criteria>
- Wave 0 artifacts exist before any Phase 24 production-code task begins.
- The LOCATE-03 scenario title contains the exact stable substring `manual picker fallback`.
- Executors have a documented sub-30-second smoke path via `--list` filters before using the broader phase suite.
</success_criteria>

<output>
After completion, create `.planning/phases/24-location-detection-foundation/24-00-SUMMARY.md`
</output>
