---
phase: 24-location-detection-foundation
plan: 02
type: execute
wave: 3
depends_on: ["24-00", "24-01"]
files_modified:
  - src/lib/components/stores/LocationPermissionCard.svelte
  - src/lib/components/stores/StoreSelector.svelte
  - src/routes/(protected)/lister/[id]/+page.svelte
autonomous: false
requirements:
  - LOCATE-01
  - LOCATE-02
  - LOCATE-03

must_haves:
  truths:
    - "The list page shows an explanation step before any geolocation permission request"
    - "The browser permission request only happens after the explicit confirm tap on the list page"
    - "Detected stores override the current manual selection while the manual picker stays visible and usable"
    - "Denied and unavailable states remain inline above the picker with retry/manual guidance"
  artifacts:
    - path: "src/lib/components/stores/LocationPermissionCard.svelte"
      provides: "Compact inline status and two-step permission UI above the manual picker"
      contains: "Slå på automatisk butikkvalg"
    - path: "src/lib/components/stores/StoreSelector.svelte"
      provides: "Always-visible manual picker with action wording when no store is selected"
      contains: "Velg butikk manuelt"
    - path: "src/routes/(protected)/lister/[id]/+page.svelte"
      provides: "List-page wiring from location session to selectedStoreId and inline recovery UX"
      contains: "LocationPermissionCard"
  key_links:
    - from: "src/routes/(protected)/lister/[id]/+page.svelte"
      to: "src/lib/location/session.svelte.ts"
      via: "button callbacks and detectedStoreId effect"
      pattern: "confirmAutomaticStore|retryLocationDetection|locationSession\\.detectedStoreId"
    - from: "src/routes/(protected)/lister/[id]/+page.svelte"
      to: "src/lib/components/stores/StoreSelector.svelte"
      via: "manual fallback remains rendered below location status"
      pattern: "StoreSelector"
    - from: "tests/location-detection.spec.ts"
      to: "src/routes/(protected)/lister/[id]/+page.svelte"
      via: "Playwright location scenarios against the real list route"
      pattern: "/lister/"
---

<objective>
Wire the Phase 24 location foundation into the list page: compact explanation/recovery UI, user-gesture permission flow, always-visible manual picker, and focused validation artifacts including the physical-iPhone installed-PWA checkpoint.

Purpose: This is the user-facing completion of `LOCATE-01`, `LOCATE-02`, and `LOCATE-03`. It turns the session created in Plan 01 into actual list-page behavior and consumes the Wave 0 validation artifacts already created for Phase 24.

Output: Inline list-page location UX, updated manual picker wording, and the installed-iPhone checkpoint wired to the existing Wave 0 Playwright suite and manual checklist.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-location-detection-foundation/24-CONTEXT.md
@.planning/phases/24-location-detection-foundation/24-RESEARCH.md
@.planning/phases/24-location-detection-foundation/24-VALIDATION.md
@.planning/phases/23-store-location-foundation/23-01-SUMMARY.md
@.planning/phases/23-store-location-foundation/23-02-SUMMARY.md

<interfaces>
From src/lib/location/proximity.ts (created in Plan 01):
```typescript
export type DetectableStore = {
  id: string
  chain: string | null
  location_name: string
  lat: number | null
  lng: number | null
}
```

From src/lib/location/session.svelte.ts (created in Plan 01):
```typescript
export type LocationSessionStatus =
  | 'idle'
  | 'explaining'
  | 'locating'
  | 'active'
  | 'permission-denied'
  | 'unavailable'

export const locationSession: {
  status: LocationSessionStatus
  detectedStoreId: string | null
  lastFailure: string | null
  showSettingsHint: boolean
  lastSample: unknown
}

export function beginLocationExplanation(): void
export function confirmAutomaticStore(stores: DetectableStore[]): Promise<void>
export function retryLocationDetection(stores: DetectableStore[]): Promise<void>
export function cancelLocationExplanation(): void
export function refreshLocationStores(stores: DetectableStore[]): void
export function stopLocationSession(): void
```

From src/lib/components/stores/StoreSelector.svelte:
```typescript
interface Props {
  stores: Store[]
  selectedStoreId: string | null
  onSelect: (id: string | null) => void
}
```

From tests/location-detection.spec.ts (created in Plan 00):
```typescript
test('permission flow waits for explicit confirm tap before first geolocation request', ...)
test('foreground poller auto-selects nearby store and resumes after visibility restore', ...)
test('manual picker fallback stays available when location is denied or unavailable', ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the list-page location UX and keep manual selection always visible</name>
  <files>src/lib/components/stores/LocationPermissionCard.svelte, src/lib/components/stores/StoreSelector.svelte, src/routes/(protected)/lister/[id]/+page.svelte</files>
  <read_first>
    src/lib/components/stores/LocationPermissionCard.svelte,
    src/lib/components/stores/StoreSelector.svelte,
    src/routes/(protected)/lister/[id]/+page.svelte,
    src/lib/location/session.svelte.ts,
    src/lib/utils/stores.ts,
    tests/location-detection.spec.ts,
    tests/helpers/location.ts,
    .planning/phases/24-location-detection-foundation/24-CONTEXT.md,
    .planning/phases/24-location-detection-foundation/24-RESEARCH.md
  </read_first>
  <action>
Create `src/lib/components/stores/LocationPermissionCard.svelte` as a compact props-driven component. It must stay inline above the picker, not a modal or blocking card.

Required props and exact copy anchors:
- `state: 'idle' | 'explaining' | 'locating' | 'active' | 'permission-denied' | 'unavailable'`
- `detectedStoreName: string | null`
- `showSettingsHint: boolean`
- `onStart`, `onConfirm`, `onCancel`, `onRetry`
- Primary idle CTA button text: `Slå på automatisk butikkvalg`
- Explanation title: `Velg butikk automatisk`
- Explanation body: `HandleAppen bruker posisjonen din bare mens appen er åpen for å finne nærmeste lagrede butikk.`
- Confirm button: `Fortsett`
- Cancel button: `Ikke nå`
- Locating copy: `Finner posisjonen din…`
- Active copy when a store is detected: `Automatisk valgt butikk: {detectedStoreName}`
- Active copy when no nearby store is detected: `Automatisk butikkvalg er på.`
- Denied copy: `Stedstilgang er avslått. Du kan prøve igjen eller velge butikk manuelt.`
- Settings hint copy when `showSettingsHint === true`: `Hvis Safari ikke spør igjen, åpne Innstillinger og gi HandleAppen stedstilgang.`
- Unavailable copy: `Vi fant ikke posisjonen din akkurat nå. Velg butikk manuelt eller prøv igjen.`

Update `src/lib/components/stores/StoreSelector.svelte`:
- Replace every no-selection label of `Ingen` / `Ingen butikk` with `Velg butikk manuelt`.
- Keep the component always renderable even when no store is selected.
- Do not remove the `Valgt` marker inside the sheet.

Update `src/routes/(protected)/lister/[id]/+page.svelte`:
- Import `LocationPermissionCard` and all session functions from `src/lib/location/session.svelte.ts`.
- Import `storeDisplayName`.
- Keep `selectedStoreId` page-owned.
- Add `const detectedStoreName = $derived(...)` using `locationSession.detectedStoreId` plus `storeDisplayName`.
- Add an effect that calls `refreshLocationStores((storesQuery.data ?? []) as DetectableStore[])` whenever the stores query changes.
- Add an effect that sets `selectedStoreId = locationSession.detectedStoreId` whenever detection returns a non-null store id different from the current selection. This is the required "detected store takes precedence over manual selection" rule.
- Render `<LocationPermissionCard ... />` above `<StoreSelector ... />`.
- Wire callbacks exactly like this:
  - first button -> `beginLocationExplanation()`
  - confirm button -> `void confirmAutomaticStore((storesQuery.data ?? []) as DetectableStore[])`
  - cancel button -> `cancelLocationExplanation()`
  - retry button -> `void retryLocationDetection((storesQuery.data ?? []) as DetectableStore[])`
- Keep the manual picker visible at all times; do not hide it when location is granted, denied, or unavailable.
- Call `stopLocationSession()` from `onDestroy`.
- Do not place any `navigator.geolocation` call directly in `+page.svelte`; all browser access must stay in `src/lib/location/*`.
  </action>
  <verify>
    <automated>npx playwright test tests/location-detection.spec.ts -g "permission flow|manual picker fallback"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/components/stores/LocationPermissionCard.svelte` contains `Slå på automatisk butikkvalg`
    - `src/lib/components/stores/LocationPermissionCard.svelte` contains `Velg butikk automatisk`
    - `src/lib/components/stores/LocationPermissionCard.svelte` contains `HandleAppen bruker posisjonen din bare mens appen er åpen`
    - `src/lib/components/stores/LocationPermissionCard.svelte` contains `Finner posisjonen din`
    - `src/lib/components/stores/StoreSelector.svelte` contains `Velg butikk manuelt`
    - `src/lib/components/stores/StoreSelector.svelte` does not contain `Ingen butikk`
    - `src/routes/(protected)/lister/[id]/+page.svelte` contains `LocationPermissionCard`
    - `src/routes/(protected)/lister/[id]/+page.svelte` contains `beginLocationExplanation`
    - `src/routes/(protected)/lister/[id]/+page.svelte` contains `confirmAutomaticStore`
    - `src/routes/(protected)/lister/[id]/+page.svelte` contains `retryLocationDetection`
    - `src/routes/(protected)/lister/[id]/+page.svelte` contains `locationSession.detectedStoreId`
    - `src/routes/(protected)/lister/[id]/+page.svelte` does not contain `navigator.geolocation`
    - `tests/location-detection.spec.ts` still contains `manual picker fallback stays available when location is denied or unavailable`
    - `npx playwright test tests/location-detection.spec.ts -g "permission flow|manual picker fallback"` passes
  </acceptance_criteria>
  <done>The list page owns a compact two-step permission flow, shows inline recovery states, keeps the manual picker visible, and lets detected stores override manual selection without calling geolocation on mount.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate installed-iPhone PWA behavior before Phase 24 closes</name>
  <files>.planning/phases/24-location-detection-foundation/24-MANUAL-CHECKLIST.md, src/routes/(protected)/lister/[id]/+page.svelte</files>
  <read_first>
    .planning/phases/24-location-detection-foundation/24-MANUAL-CHECKLIST.md,
    src/routes/(protected)/lister/[id]/+page.svelte,
    tests/location-detection.spec.ts,
    .planning/phases/24-location-detection-foundation/24-CONTEXT.md,
    .planning/phases/24-location-detection-foundation/24-VALIDATION.md
  </read_first>
  <action>Run the automated Phase 24 browser suite first, then perform the physical-device checklist from `.planning/phases/24-location-detection-foundation/24-MANUAL-CHECKLIST.md` on an installed iPhone PWA. This checkpoint exists only for the WebKit installed-app prompt behavior and foreground background/resume semantics that browser automation cannot prove.</action>
  <verify>
    <automated>npx playwright test tests/location-detection.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/location-detection.spec.ts` passes before manual verification starts
    - The physical checklist confirms the first permission prompt appears only after tapping `Fortsett`
    - The physical checklist confirms manual picker access remains available after denied and unavailable cases
    - The physical checklist confirms backgrounding and reopening the installed app triggers an immediate refresh attempt
    - The final human response is exactly `approved` or includes concrete failure notes tied to checklist steps
  </acceptance_criteria>
  <what-built>List-page location UX, focused Playwright coverage, and a manual checklist for installed-iPhone PWA validation.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open a real list route on the installed iPhone PWA.
    2. Confirm no location prompt appears on page load.
    3. Tap `Slå på automatisk butikkvalg` and verify the inline explanation appears above the manual picker.
    4. Tap `Fortsett` and confirm the iOS location prompt appears.
    5. Deny once and verify the inline denied message plus retry button appear while `Velg butikk manuelt` remains visible.
    6. Retry once more and verify the Safari Settings hint appears if the browser no longer re-prompts.
    7. Re-enable permission, grant access, and confirm a nearby saved store becomes selected automatically.
    8. Background the installed app for at least 30 seconds, reopen it, and confirm the app refreshes location immediately without a full page reload.
    9. Temporarily make location unavailable and confirm the unavailable message still leaves manual selection usable.
  </how-to-verify>
  <done>Installed-iPhone PWA behavior matches the locked Phase 24 decisions and the tester responds `approved`.</done>
  <resume-signal>Type `approved` or list the failing checklist step numbers.</resume-signal>
</task>

</tasks>

<verification>
- `npx playwright test tests/location-detection.spec.ts`
- `rg -n "Velg butikk manuelt|Slå på automatisk butikkvalg|Fortsett" src/lib/components/stores src/routes/(protected)/lister/[id]/+page.svelte .planning/phases/24-location-detection-foundation/24-MANUAL-CHECKLIST.md`
</verification>

<success_criteria>
- The permission prompt is gated behind a two-step list-page interaction.
- Automatic detection uses the Plan 01 session and can override manual selection without hiding the manual picker.
- Inline denied and unavailable states stay visible until detection succeeds.
- The installed-iPhone checklist exists and the checkpoint cannot pass without explicit human approval.
</success_criteria>

<output>
After completion, create `.planning/phases/24-location-detection-foundation/24-02-SUMMARY.md`
</output>
