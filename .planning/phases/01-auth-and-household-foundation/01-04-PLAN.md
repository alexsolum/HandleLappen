---
phase: 01-auth-and-household-foundation
plan: 04
type: execute
wave: gap-closure
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/routes/+layout.svelte
  - src/routes/(protected)/+page.svelte
  - src/routes/(protected)/husstand/+page.svelte
  - tests/auth.spec.ts
autonomous: true
requirements:
  - AUTH-03

must_haves:
  truths:
    - "An authenticated user can sign out from the protected experience without clearing cookies manually"
    - "Signing out ends the Supabase session and returns the user to /logg-inn"
    - "After sign-out, visiting /husstand redirects to /logg-inn?next=%2Fhusstand"
  artifacts:
    - path: "src/routes/+layout.svelte"
      provides: "Shared client-side sign-out action wired to the existing Supabase session client"
    - path: "src/routes/(protected)/+page.svelte"
      provides: "Visible logout affordance on the protected landing page"
    - path: "src/routes/(protected)/husstand/+page.svelte"
      provides: "Visible logout affordance on the household screen used during UAT"
    - path: "tests/auth.spec.ts"
      provides: "Coverage for sign-out and post-logout protected-route redirect"
---

<objective>
Close the Phase 1 UAT gap by adding a visible sign-out path to the authenticated experience, wiring it to `supabase.auth.signOut()`, and verifying that protected routes redirect correctly once the session is cleared.

Purpose: The route guard itself already exists, but the user could not exercise it because no logout path was implemented. This plan makes the signed-out state reachable and testable.

Output: Authenticated pages expose a logout control, logout clears the session, and sign-out behavior is covered by smoke-level auth tests.
</objective>

<context>
@.planning/phases/01-auth-and-household-foundation/01-UAT.md
@.planning/phases/01-auth-and-household-foundation/01-02-PLAN.md
@.planning/phases/01-auth-and-household-foundation/01-03-PLAN.md

<gap>
Truth: When signed out and opening /husstand directly, the app should redirect to /logg-inn with next=%2Fhusstand.
Diagnosed root cause: No sign-out control or `supabase.auth.signOut()` call exists anywhere in the current UI, so users cannot return to a signed-out state.
</gap>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a reusable sign-out action to the authenticated shell</name>
  <files>
    src/routes/+layout.svelte
  </files>
  <action>
    Update `src/routes/+layout.svelte` so authenticated pages can trigger logout through the existing client-side Supabase instance.

    Requirements:
    - Keep the existing auth-state invalidation behavior.
    - Add a `signOut` function that calls `await data.supabase.auth.signOut()`.
    - After sign-out succeeds, navigate to `/logg-inn`.
    - Make the sign-out function available to protected page content in a simple, maintainable way. Prefer Svelte context or a top-level authenticated header slot over duplicating logout logic in multiple pages.
    - If sign-out fails, surface a minimal user-visible error state instead of silently swallowing it.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    Authenticated UI can invoke a single shared logout action without duplicating Supabase logout code.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expose logout controls on the current protected pages</name>
  <files>
    src/routes/(protected)/+page.svelte,
    src/routes/(protected)/husstand/+page.svelte
  </files>
  <action>
    Add a clearly labeled logout button to the protected home page and the household page.

    Requirements:
    - The control text should be Norwegian, e.g. `Logg ut`.
    - Place it in an obvious location users will actually find during UAT.
    - Reuse the shared sign-out action from Task 1 rather than reimplementing logout inline.
    - Preserve the existing Phase 1 visual language.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    Users can reach a logout action from the screens they use after signing in and onboarding.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add auth coverage for logout and redirect-after-logout</name>
  <files>
    tests/auth.spec.ts
  </files>
  <action>
    Replace or extend the existing auth smoke stubs so the suite explicitly covers the missing UAT behavior.

    Requirements:
    - Add a smoke test for logout that signs in, triggers `Logg ut`, and confirms the user lands on `/logg-inn`.
    - Add a follow-up assertion that visiting `/husstand` after logout redirects to `/logg-inn?next=%2Fhusstand`.
    - If the test still depends on seeded auth fixtures, keep the conditional skip pattern already used elsewhere, but the test structure and assertions must be present.
  </action>
  <verify>
    <automated>npx playwright test tests/auth.spec.ts --grep "sign out|logout"</automated>
  </verify>
  <done>
    Test coverage exists for the exact logout gap found in UAT.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify logout flow manually</name>
  <what-built>
    A visible logout action on authenticated screens that clears the Supabase session and returns the user to the login page.
  </what-built>
  <how-to-verify>
    1. Sign in with a valid account and open `/husstand`.
    2. Click `Logg ut`.
    3. Expected: you are sent to `/logg-inn`.
    4. Without signing in again, open `/husstand`.
    5. Expected: redirect to `/logg-inn?next=%2Fhusstand`.

    Type `approved` if all steps pass, or describe the failure.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issue</resume-signal>
  <action>Human verification only.</action>
  <verify>
    <automated>echo "Human checkpoint - verify logout flow"</automated>
  </verify>
  <done>
    Logout is reachable, clears the session, and protected-route redirect works after logout.
  </done>
</task>

</tasks>

<verification>
1. `npm run check` passes.
2. `npm run build` passes.
3. Auth coverage includes logout and redirect-after-logout assertions.
4. Manual verification confirms `/husstand` redirects to `/logg-inn?next=%2Fhusstand` after logout.
</verification>

<success_criteria>
- Users can sign out from the authenticated UI.
- Logout clears the Supabase session without manual cookie clearing.
- The original UAT protected-route redirect can be reproduced and passes after logout.
</success_criteria>
