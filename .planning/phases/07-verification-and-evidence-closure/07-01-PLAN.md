---
phase: 07-verification-and-evidence-closure
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
gap_closure: true
files_modified:
  - .planning/phases/04-barcode-scanning/04-VERIFICATION.md
requirements:
  - BARC-01
  - BARC-02
  - BARC-03
  - BARC-04

must_haves:
  truths:
    - "04-VERIFICATION.md maps BARC-01 through BARC-04 individually rather than summarizing the barcode phase as a whole"
    - "Device-only scan claims remain partial or missing unless a specific prior manual checkpoint proves Android Chrome, iPhone Safari, or iPhone standalone behavior"
    - "Browser and function automation are treated as primary evidence for provider fallback, normalization, and auto-fill behavior"
    - "Residual risk is explicit when barcode evidence stops short of fully proving real-device/PWA behavior"
  artifacts:
    - path: ".planning/phases/04-barcode-scanning/04-VERIFICATION.md"
      provides: "formal barcode requirement-to-evidence mapping, residual-risk section, and phase verdict"
    - path: "tests/barcode.spec.ts"
      provides: "browser-level scan, fallback, manual EAN, and not-found evidence inputs"
    - path: "supabase/functions/barcode-lookup/index.test.ts"
      provides: "provider fallback, cache, and normalization evidence inputs"
  key_links:
    - from: ".planning/phases/04-barcode-scanning/04-VERIFICATION.md"
      to: "tests/barcode.spec.ts"
      via: "BARC-01 and BARC-04 app-flow evidence references"
      pattern: "BARC-01"
    - from: ".planning/phases/04-barcode-scanning/04-VERIFICATION.md"
      to: "supabase/functions/barcode-lookup/index.test.ts"
      via: "BARC-02 and BARC-03 backend/provider evidence references"
      pattern: "BARC-02"
---

<objective>
Produce `.planning/phases/04-barcode-scanning/04-VERIFICATION.md` as the formal Phase 4 verification artifact, mapping every barcode requirement to current code, automated tests, prior validation notes, and any valid manual checkpoint evidence.

Purpose: Phase 4 already has delivery artifacts and targeted automation, but it still fails milestone audit because those materials are not synthesized into a requirement-level verification document. This plan closes that audit gap without turning back into feature work.
Output: One honest verification artifact with evidence inventory, per-requirement coverage verdicts, explicit residual risks, and a clear phase verdict for BARC-01..04.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-verification-and-evidence-closure/07-CONTEXT.md
@.planning/phases/07-verification-and-evidence-closure/07-RESEARCH.md
@.planning/phases/07-verification-and-evidence-closure/07-VALIDATION.md
@.planning/phases/04-barcode-scanning/04-CONTEXT.md
@.planning/phases/04-barcode-scanning/04-RESEARCH.md
@.planning/phases/04-barcode-scanning/04-VALIDATION.md
@.planning/phases/04-barcode-scanning/04-01-SUMMARY.md
@.planning/phases/04-barcode-scanning/04-02-SUMMARY.md

<interfaces>
Primary evidence inputs:
- `tests/barcode.spec.ts`
- `supabase/functions/barcode-lookup/index.test.ts`
- `src/lib/queries/barcode.ts`
- `src/lib/components/barcode/BarcodeScannerSheet.svelte`
- `src/lib/components/barcode/BarcodeLookupSheet.svelte`
- `src/routes/(protected)/lister/[id]/+page.svelte`

Locked verification rules:
- Barcode verification is requirement-level, not phase-summary-only.
- Automated evidence is primary wherever it exists.
- Code inspection may support a claim but does not replace missing proof.
- Device-only claims stay partial or missing unless a concrete manual checkpoint already exists.
- The output document must make `covered`, `partial`, and `missing` explicit per requirement.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Phase 4 evidence sources and determine honest requirement coverage</name>
  <files>
    .planning/phases/04-barcode-scanning/04-VERIFICATION.md
  </files>
  <action>
Read the Phase 4 planning and execution artifacts plus the current barcode code/tests. Build a working evidence matrix for:
- `BARC-01`: open camera view, scan flow, and add-to-list path
- `BARC-02`: Kassal.app primary with Open Food Facts fallback through the Edge Function
- `BARC-03`: Gemini-assisted normalization/category identification
- `BARC-04`: auto-filled name/category from scan result

For each requirement, identify:
1. direct automated evidence
2. supporting code references
3. prior validation or manual checkpoint notes
4. unresolved proof gaps

Do not smooth over real-device gaps. If iOS Safari or standalone PWA scan behavior lacks a specific manual checkpoint, record that as partial or missing evidence rather than inferring success from desktop/browser automation.
  </action>
  <verify>
    <automated>rg -n "BARC-01|BARC-02|BARC-03|BARC-04" tests/barcode.spec.ts supabase/functions/barcode-lookup/index.test.ts .planning/phases/04-barcode-scanning/04-VALIDATION.md && rg -n "barcode|lookup|manual|scan|canonicalCategory|found|not found" src/lib/queries/barcode.ts src/lib/components/barcode/BarcodeScannerSheet.svelte src/lib/components/barcode/BarcodeLookupSheet.svelte src/routes/(protected)/lister/[id]/+page.svelte</automated>
  </verify>
  <done>A complete evidence matrix exists for all four barcode requirements, including any device-only proof gaps that must remain explicit in the final verification artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Write 04-VERIFICATION.md with requirement mapping, residual risks, and verdict</name>
  <files>
    .planning/phases/04-barcode-scanning/04-VERIFICATION.md
  </files>
  <action>
Create `.planning/phases/04-barcode-scanning/04-VERIFICATION.md` using the established verification-artifact structure:
1. phase summary and verification date
2. evidence inventory
3. requirement-by-requirement mapping
4. residual risks / open proof gaps
5. phase verdict

For every requirement row or section, include:
- requirement ID and short statement
- evidence type
- concrete evidence references
- coverage verdict: `covered`, `partial`, or `missing`
- notes explaining why

Keep the document documentation-first. Only note follow-up work if the audit truly reveals a blocking mismatch; do not reopen Phase 4 implementation inside this plan.
  </action>
  <verify>
    <automated>rg -n "BARC-01|BARC-02|BARC-03|BARC-04|covered|partial|missing|Residual|Verdict" .planning/phases/04-barcode-scanning/04-VERIFICATION.md</automated>
  </verify>
  <done>`04-VERIFICATION.md` exists and each barcode requirement is explicitly mapped to concrete evidence with an honest coverage verdict.</done>
</task>

<task type="auto">
  <name>Task 3: Freshness-check the cited automation and finalize the audit-ready verdict</name>
  <files>
    .planning/phases/04-barcode-scanning/04-VERIFICATION.md
  </files>
  <action>
Run the targeted barcode suites used by the document and confirm the written evidence still matches current repo behavior. If any test outcome or code path contradicts the draft verification doc, update the document rather than forcing a pass statement.

The final verdict must clearly distinguish:
- what Phase 4 proves strongly today
- what remains partial because device/manual evidence is incomplete
- whether the artifact is sufficient for Phase 8 traceability reconciliation
  </action>
  <verify>
    <automated>deno test --allow-env --allow-net --allow-read supabase/functions/barcode-lookup/index.test.ts && npx playwright test tests/barcode.spec.ts --reporter=list</automated>
  </verify>
  <done>The barcode verification document is aligned with current automated evidence and ends with a clear, audit-ready phase verdict plus explicit residual risk handling.</done>
</task>

</tasks>

<verification>
1. `rg -n "BARC-01|BARC-02|BARC-03|BARC-04" .planning/phases/04-barcode-scanning/04-VERIFICATION.md` shows every Phase 4 requirement explicitly mapped
2. `deno test --allow-env --allow-net --allow-read supabase/functions/barcode-lookup/index.test.ts` passes or any failure is reflected honestly in the verification doc
3. `npx playwright test tests/barcode.spec.ts --reporter=list` passes or any failure is reflected honestly in the verification doc
4. `04-VERIFICATION.md` contains `covered`, `partial`, or `missing` for every requirement and a residual-risk section
5. No device-only claim is marked covered without a specific cited manual checkpoint note
</verification>

<success_criteria>
- `04-VERIFICATION.md` exists and is requirement-driven rather than summary-driven
- `BARC-01`, `BARC-02`, `BARC-03`, and `BARC-04` each have concrete evidence references and an explicit verdict
- Provider fallback, Gemini normalization, and auto-fill behavior are tied to tests/code instead of broad prose
- Any barcode real-device or standalone-PWA evidence gap is called out plainly
- The final phase verdict is honest and usable for downstream audit reconciliation
</success_criteria>

<must_have_self_check>
- No barcode requirement is marked covered without both a current code path and at least one cited evidence source
- Manual-only scan claims remain `partial` or `missing` unless a specific checkpoint note proves them
- The final verdict does not over-claim iPhone Safari or installed PWA scan behavior
</must_have_self_check>

<output>
After completion, create `.planning/phases/07-verification-and-evidence-closure/07-01-SUMMARY.md`.
</output>
