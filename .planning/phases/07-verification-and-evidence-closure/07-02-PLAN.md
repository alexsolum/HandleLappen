---
phase: 07-verification-and-evidence-closure
plan: 02
type: execute
wave: 1
depends_on: []
autonomous: true
gap_closure: true
files_modified:
  - .planning/phases/05-pwa-and-offline-support/05-VERIFICATION.md
requirements:
  - PWAF-01
  - PWAF-02

must_haves:
  truths:
    - "05-VERIFICATION.md maps PWAF-01 and PWAF-02 separately and does not treat generic PWA setup as proof of installability"
    - "Installability and standalone claims remain partial or missing unless a concrete manual checkpoint note proves actual Add-to-Home-Screen and standalone behavior"
    - "Offline readability, queueing, and replay evidence reuses targeted automation and existing UAT where the mapping remains accurate"
    - "Residual risk is explicit for platform-specific install behavior and Safari next-open replay nuances"
  artifacts:
    - path: ".planning/phases/05-pwa-and-offline-support/05-VERIFICATION.md"
      provides: "formal PWA/offline requirement mapping, manual-evidence handling, and phase verdict"
    - path: "tests/pwa.spec.ts"
      provides: "browser-side PWA manifest and registration evidence inputs"
    - path: "tests/offline.spec.ts"
      provides: "offline cached-list, pending-sync, and replay evidence inputs"
    - path: ".planning/phases/05-pwa-and-offline-support/05-UAT.md"
      provides: "manual/UAT evidence inputs for installability and offline shopping behavior"
  key_links:
    - from: ".planning/phases/05-pwa-and-offline-support/05-VERIFICATION.md"
      to: "tests/offline.spec.ts"
      via: "PWAF-02 offline-readability and sync-path evidence references"
      pattern: "PWAF-02"
    - from: ".planning/phases/05-pwa-and-offline-support/05-VERIFICATION.md"
      to: ".planning/phases/05-pwa-and-offline-support/05-UAT.md"
      via: "PWAF-01 manual-install and standalone evidence references"
      pattern: "PWAF-01"
---

<objective>
Produce `.planning/phases/05-pwa-and-offline-support/05-VERIFICATION.md` as the formal Phase 5 verification artifact, mapping the PWA and offline requirements to current tests, code, and any valid manual/UAT evidence.

Purpose: Phase 5 delivery artifacts already exist, but audit closure requires one document that states exactly what PWAF-01 and PWAF-02 are proven by, what remains manual-only, and what residual risk still exists.
Output: One audit-ready verification artifact for Phase 5 with explicit requirement mapping, evidence inventory, residual-risk handling, and a clear phase verdict.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-verification-and-evidence-closure/07-CONTEXT.md
@.planning/phases/07-verification-and-evidence-closure/07-RESEARCH.md
@.planning/phases/07-verification-and-evidence-closure/07-VALIDATION.md
@.planning/phases/05-pwa-and-offline-support/05-CONTEXT.md
@.planning/phases/05-pwa-and-offline-support/05-RESEARCH.md
@.planning/phases/05-pwa-and-offline-support/05-VALIDATION.md
@.planning/phases/05-pwa-and-offline-support/05-UAT.md
@.planning/phases/05-pwa-and-offline-support/05-01-SUMMARY.md
@.planning/phases/05-pwa-and-offline-support/05-02-SUMMARY.md
@.planning/phases/05-pwa-and-offline-support/05-03-SUMMARY.md

<interfaces>
Primary evidence inputs:
- `tests/pwa.spec.ts`
- `tests/offline.spec.ts`
- `src/service-worker.ts`
- `src/routes/(protected)/+layout.svelte`
- `src/lib/offline/queue.ts`
- `src/lib/stores/offline.svelte.ts`

Locked verification rules:
- `PWAF-01` requires manual evidence for installability and standalone behavior; manifest/service-worker proof alone is not enough.
- `PWAF-02` can rely more heavily on targeted automation plus UAT if the mapping is explicit and still matches current code.
- The verification artifact must state `covered`, `partial`, or `missing` per requirement.
- Existing UAT may be reused as first-class evidence only after a quick code-to-evidence consistency check.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Phase 5 evidence sources and split browser proof from manual install proof</name>
  <files>
    .planning/phases/05-pwa-and-offline-support/05-VERIFICATION.md
  </files>
  <action>
Read the Phase 5 planning, summaries, UAT, tests, and relevant PWA/offline code. Build an evidence matrix for:
- `PWAF-01`: installability and standalone-mode behavior
- `PWAF-02`: last cached shopping list remains readable offline or on poor signal

Treat `PWAF-01` as two linked but distinct evidence buckets:
1. technical groundwork visible in code/tests (manifest, service worker, registration)
2. actual platform install/standalone proof from manual checkpoints

Do not count the first bucket alone as full requirement coverage.
  </action>
  <verify>
    <automated>rg -n "PWAF-01|PWAF-02|install|standalone|offline|pending" tests/pwa.spec.ts tests/offline.spec.ts .planning/phases/05-pwa-and-offline-support/05-UAT.md .planning/phases/05-pwa-and-offline-support/05-VALIDATION.md && rg -n "manifest|standalone|offline|queue|sync|service worker|pending" src/service-worker.ts src/routes/(protected)/+layout.svelte src/lib/offline/queue.ts src/lib/stores/offline.svelte.ts</automated>
  </verify>
  <done>A complete evidence matrix exists for both PWA requirements, with install/manual evidence clearly separated from browser-automation evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Write 05-VERIFICATION.md with explicit requirement-to-evidence mapping</name>
  <files>
    .planning/phases/05-pwa-and-offline-support/05-VERIFICATION.md
  </files>
  <action>
Create `.planning/phases/05-pwa-and-offline-support/05-VERIFICATION.md` with:
1. phase summary and verification date
2. evidence inventory
3. requirement-by-requirement mapping
4. residual risks / open proof gaps
5. phase verdict

For each requirement, record:
- the requirement ID and short statement
- evidence type
- concrete test/code/UAT references
- coverage verdict: `covered`, `partial`, or `missing`
- notes including any platform-specific caveats

Keep the document focused on evidence closure. If code/UAT drift is found, document it honestly and avoid broad pass language that exceeds the proof.
  </action>
  <verify>
    <automated>rg -n "PWAF-01|PWAF-02|covered|partial|missing|Residual|Verdict" .planning/phases/05-pwa-and-offline-support/05-VERIFICATION.md</automated>
  </verify>
  <done>`05-VERIFICATION.md` exists and gives PWAF-01 and PWAF-02 explicit, auditable evidence mappings with residual risk handling.</done>
</task>

<task type="auto">
  <name>Task 3: Rerun targeted PWA/offline suites and finalize the honest verdict</name>
  <files>
    .planning/phases/05-pwa-and-offline-support/05-VERIFICATION.md
  </files>
  <action>
Run the targeted Phase 5 suites used by the document and compare the results against the drafted verification claims. Update the artifact if any evidence reference is stale, overstated, or contradicted by the current repo state.

The final verdict must specifically answer:
- whether Phase 5 proves offline-readability and queued-sync behavior strongly enough for audit
- whether installability and standalone behavior are fully covered, only partially covered, or still missing on any platform
  </action>
  <verify>
    <automated>npx playwright test tests/pwa.spec.ts tests/offline.spec.ts --reporter=list</automated>
  </verify>
  <done>The PWA verification document is aligned with current targeted automation and ends with a clear, audit-ready verdict plus explicit platform-specific residual risk notes.</done>
</task>

</tasks>

<verification>
1. `rg -n "PWAF-01|PWAF-02" .planning/phases/05-pwa-and-offline-support/05-VERIFICATION.md` shows both requirements explicitly mapped
2. `npx playwright test tests/pwa.spec.ts tests/offline.spec.ts --reporter=list` passes or any failure is reflected honestly in the verification doc
3. `05-VERIFICATION.md` contains `covered`, `partial`, or `missing` for both requirements and names residual risk directly
4. No installability or standalone claim is marked covered without a specific cited manual/UAT checkpoint
5. The document distinguishes technical PWA groundwork from actual install/use proof
</verification>

<success_criteria>
- `05-VERIFICATION.md` exists and is requirement-driven
- `PWAF-01` and `PWAF-02` each have concrete evidence references and an explicit verdict
- Offline cached-list and queued-sync behavior reuse existing tests/UAT as first-class evidence where valid
- Platform install and standalone gaps, if any, are stated explicitly rather than implied away
- The final phase verdict is specific enough for Phase 8 traceability reconciliation
</success_criteria>

<must_have_self_check>
- `PWAF-01` installability is not treated as fully covered by manifest and service-worker evidence alone
- `PWAF-02` is grounded in current offline queue code and targeted evidence, not only prior summaries
- Any missing manual install or standalone proof remains explicit as residual risk or partial coverage
</must_have_self_check>

<output>
After completion, create `.planning/phases/07-verification-and-evidence-closure/07-02-SUMMARY.md`.
</output>
