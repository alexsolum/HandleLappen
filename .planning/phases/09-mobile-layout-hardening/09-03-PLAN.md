---
phase: 09-mobile-layout-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - tests/mobile-layout.spec.ts
  - tests/barcode.spec.ts
  - tests/categories.spec.ts
autonomous: true
requirements:
  - MOBL-01
  - MOBL-02
  - MOBL-03

must_haves:
  truths:
    - "Phase 9 has focused automated coverage for mobile viewport overflow, mobile sheet behavior, and bottom-dock visibility"
    - "Automated tests assert that the signed-in shell does not become horizontally scrollable on phone-sized viewports"
    - "Automated tests cover at least one long-form sheet with visible actions and the pinned dock on a list screen"
    - "Residual real-device checks for safe area and keyboard behavior are called out explicitly rather than assumed"
  artifacts:
    - path: "tests/mobile-layout.spec.ts"
      provides: "Dedicated Phase 9 mobile layout coverage"
    - path: "tests/barcode.spec.ts"
      provides: "Updated barcode-sheet coverage if selectors or sheet structure changed"
    - path: "tests/categories.spec.ts"
      provides: "Updated list-screen coverage if bottom-dock or sheet selectors changed"
  key_links:
    - from: "tests/mobile-layout.spec.ts"
      to: "MOBL-01, MOBL-02, MOBL-03"
      via: "viewport-width assertions and fixed-element visibility checks"
      pattern: "scrollWidth"
---

<objective>
Add Phase 9 verification coverage after the sheet and dock changes land. This plan creates the mobile-focused automated test file, adjusts existing tests if selectors changed, and makes the manual-only checks explicit for real devices.

Purpose: Close the phase with evidence, not only UI changes. This plan is the automated and manual verification layer for MOBL-01..03.

Output: Dedicated mobile layout Playwright coverage and updated existing tests where the dock/sheet refactor changed selectors or structure.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-mobile-layout-hardening/09-CONTEXT.md
@.planning/phases/09-mobile-layout-hardening/09-RESEARCH.md
@.planning/phases/09-mobile-layout-hardening/09-VALIDATION.md
@.planning/phases/09-mobile-layout-hardening/09-01-SUMMARY.md
@.planning/phases/09-mobile-layout-hardening/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create dedicated mobile layout Playwright coverage</name>
  <files>tests/mobile-layout.spec.ts</files>
  <action>
Create a new Playwright spec focused on mobile layout hardening.

Coverage requirements:
- use a phone-sized viewport explicitly in the test setup
- assert no horizontal overflow on the signed-in shell / list screen
- open at least one long-form sheet and assert its primary actions are visible without scrolling to the very end
- assert the bottom dock is visible and pinned on a list screen

Useful assertions:
- `document.documentElement.scrollWidth <= window.innerWidth`
- bounding-box or viewport-visibility checks for dock and action buttons

Favor stable selectors and add `data-testid` hooks in implementation plans if needed.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/mobile-layout.spec.ts</automated>
  </verify>
  <done>Phase 9 has a dedicated automated spec that exercises mobile viewport overflow, sheet visibility, and dock visibility.</done>
</task>

<task type="auto">
  <name>Task 2: Update adjacent E2E specs and record residual manual checks</name>
  <files>tests/barcode.spec.ts, tests/categories.spec.ts</files>
  <action>
Update existing barcode/list/category tests if the dock or sheet refactor changed selectors, text structure, or assumptions about mobile layout. Keep the changes minimal and targeted.

Also ensure the execution summary or verification notes explicitly call out the real-device checks that automation does not replace:
- iOS safe-area behavior in standalone mode
- one-handed dock ergonomics
- keyboard interaction with fixed bottom layers
  </action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <done>The new mobile layout coverage passes and the existing E2E suite remains aligned with the refactored dock/sheet structure.</done>
</task>

</tasks>

<verification>
1. `npm run test:e2e -- tests/mobile-layout.spec.ts`
2. `npm run test:e2e`
3. Manual phone check: verify dock ergonomics, safe area, and keyboard behavior against the explicit Phase 9 checklist.
</verification>

<success_criteria>
- Dedicated Playwright coverage exists for MOBL-01..03
- Existing barcode/category tests still pass after any selector updates
- Manual-only mobile checks are explicitly documented for execution/UAT
</success_criteria>
