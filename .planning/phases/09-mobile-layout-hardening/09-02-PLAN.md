---
phase: 09-mobile-layout-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/lib/components/lists/BottomNav.svelte
  - src/lib/components/items/ItemInput.svelte
  - src/routes/(protected)/+layout.svelte
autonomous: true
requirements:
  - MOBL-02
  - MOBL-03

must_haves:
  truths:
    - "Bottom navigation is a pinned mobile dock rather than a thin text row"
    - "Dock links are icon-based and provide larger touch targets suitable for thumb use"
    - "The bottom dock respects safe-area spacing in mobile browser and PWA standalone contexts"
    - "The dock, item input bar, and shell spacing behave as one coordinated bottom stack without overlap or width leakage"
  artifacts:
    - path: "src/lib/components/lists/BottomNav.svelte"
      provides: "Pinned icon-based bottom dock with safe-area padding and larger hit areas"
    - path: "src/lib/components/items/ItemInput.svelte"
      provides: "Bottom input bar offset correctly above the dock"
    - path: "src/routes/(protected)/+layout.svelte"
      provides: "Final bottom-stack spacing and toast offset aligned to dock height"
  key_links:
    - from: "BottomNav.svelte"
      to: "ItemInput.svelte"
      via: "shared bottom offset / stacking relationship"
      pattern: "bottom-"
    - from: "BottomNav.svelte"
      to: "safe area env() spacing"
      via: "dock padding and bottom inset behavior"
      pattern: "safe-area"
---

<objective>
Turn the current fixed bottom nav into a true mobile dock with icons, larger touch targets, and safe-area-aware spacing, then restack the add-item bar and shell spacing around the final dock height.

Purpose: This plan finishes MOBL-03 and the bottom-stack part of MOBL-02. It should only run after the shell/sheet contract from 09-01 is stable.

Output: A pinned icon dock, updated bottom input positioning, and a protected shell whose fixed layers no longer collide or create layout drift on mobile.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-mobile-layout-hardening/09-CONTEXT.md
@.planning/phases/09-mobile-layout-hardening/09-RESEARCH.md
@.planning/phases/09-mobile-layout-hardening/09-VALIDATION.md
@.planning/phases/09-mobile-layout-hardening/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild BottomNav as a pinned icon dock</name>
  <files>src/lib/components/lists/BottomNav.svelte</files>
  <action>
Replace the current text-only nav row with a dock-style mobile nav:
- keep it pinned to the bottom of the viewport
- introduce icons for each primary tab
- keep labels, but make the dock icon-led instead of text-only
- increase touch-target size to a mobile-friendly minimum
- respect bottom safe area using `env(safe-area-inset-bottom)`
- preserve the offline badge / pending badge behavior on the Lister tab

Do not add a UI library just to get a dock look. Implement within the existing Svelte/Tailwind patterns.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The nav looks and behaves like a bottom dock on mobile, with icons and larger touch targets, while preserving current routing and offline indicators.</done>
</task>

<task type="auto">
  <name>Task 2: Restack the fixed bottom layers around the final dock height</name>
  <files>src/lib/components/items/ItemInput.svelte, src/routes/(protected)/+layout.svelte</files>
  <action>
Adjust the fixed add-item bar and protected layout spacing to match the new dock height.

Requirements:
- `ItemInput.svelte` must sit cleanly above the dock without overlap
- shell bottom padding must be updated to keep scrollable content accessible
- sync toast offset must still clear both the dock and the fixed item input
- no new horizontal overflow can be introduced by the wider/taller dock treatment

Keep the existing list-entry behavior unchanged; this plan is layout-only.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The dock, item input bar, and toast occupy one stable bottom stack, and list content remains reachable above them.</done>
</task>

</tasks>

<verification>
1. `npm run check`
2. Manual narrow-viewport check: open a list and verify the dock stays pinned at the bottom while the item input remains above it.
3. Verify the offline/pending badge still renders correctly on the Lister dock button.
</verification>

<success_criteria>
- Bottom navigation is icon-based, pinned, and more tappable on mobile
- Safe-area spacing is respected in the dock
- The add-item bar and dock do not overlap or push content off-screen
- Offline badges still function on the dock
</success_criteria>
