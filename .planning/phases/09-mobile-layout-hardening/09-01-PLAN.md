---
phase: 09-mobile-layout-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/(protected)/+layout.svelte
  - src/lib/components/items/ItemDetailSheet.svelte
  - src/lib/components/items/CategoryPickerModal.svelte
  - src/lib/components/barcode/BarcodeScannerSheet.svelte
  - src/lib/components/barcode/ManualEanEntrySheet.svelte
  - src/lib/components/barcode/BarcodeLookupSheet.svelte
autonomous: true
requirements:
  - MOBL-01
  - MOBL-02

must_haves:
  truths:
    - "All Phase 9 bottom sheets use one mobile-safe shell pattern instead of drifting class-by-class"
    - "Sheets are inset from the screen edges, capped in height, and do not render content past the viewport width"
    - "Long sheet content scrolls internally while the action area remains visible"
    - "The signed-in shell prevents accidental horizontal scrolling on mobile viewports"
  artifacts:
    - path: "src/routes/(protected)/+layout.svelte"
      provides: "Protected app shell with horizontal-overflow guard and bottom-stack spacing baseline"
      contains: "overflow-x"
    - path: "src/lib/components/items/ItemDetailSheet.svelte"
      provides: "Normalized mobile sheet shell for item editing"
    - path: "src/lib/components/items/CategoryPickerModal.svelte"
      provides: "Normalized mobile sheet shell for category picking"
    - path: "src/lib/components/barcode/BarcodeScannerSheet.svelte"
      provides: "Normalized mobile sheet shell for scanner flow"
    - path: "src/lib/components/barcode/ManualEanEntrySheet.svelte"
      provides: "Normalized mobile sheet shell for manual EAN flow"
    - path: "src/lib/components/barcode/BarcodeLookupSheet.svelte"
      provides: "Normalized mobile sheet shell with visible action area for lookup confirmation"
  key_links:
    - from: "src/routes/(protected)/+layout.svelte"
      to: "BottomNav and ItemInput fixed layers"
      via: "shared bottom padding and viewport shell behavior"
      pattern: "pb-"
    - from: "sheet components"
      to: "09-CONTEXT.md"
      via: "shared inset-card + capped-height + visible-footer contract"
      pattern: "max-h-[calc(100dvh"
---

<objective>
Create the shared mobile layout contract for Phase 9 by hardening the protected shell and normalizing all existing bottom-sheet dialogs. This plan handles the viewport-width and horizontal-overflow side of the phase before the bottom dock itself is redesigned.

Purpose: Satisfy MOBL-01 and the shell-level part of MOBL-02. Without a common sheet/layout contract, later dock work would still sit on top of unstable mobile UI.

Output: The protected shell clips horizontal overflow safely, and every existing sheet component uses the same inset, capped, internal-scroll mobile pattern with visible actions.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-mobile-layout-hardening/09-CONTEXT.md
@.planning/phases/09-mobile-layout-hardening/09-RESEARCH.md
@.planning/phases/09-mobile-layout-hardening/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the protected shell against mobile horizontal overflow</name>
  <files>src/routes/(protected)/+layout.svelte</files>
  <action>
Audit the protected shell and convert it from a generic `min-h-screen bg-gray-50` wrapper into a mobile-safe shell that explicitly clips horizontal overflow while preserving vertical scrolling.

Requirements:
- Add a top-level overflow-x guard (`overflow-x-clip` preferred, `overflow-x-hidden` acceptable fallback).
- Recalculate `main` bottom padding so it is no longer tuned only for the current thin nav.
- Preserve existing sync toast behavior, but ensure the toast is positioned above the future dock height rather than hardcoding a brittle offset.
- Do not change route logic or QueryClient wiring.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>The protected shell prevents sideways scroll from fixed/mobile children and exposes a stable bottom-spacing baseline for later dock work.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize all bottom-sheet components to one mobile-safe contract</name>
  <files>
    src/lib/components/items/ItemDetailSheet.svelte,
    src/lib/components/items/CategoryPickerModal.svelte,
    src/lib/components/barcode/BarcodeScannerSheet.svelte,
    src/lib/components/barcode/ManualEanEntrySheet.svelte,
    src/lib/components/barcode/BarcodeLookupSheet.svelte
  </files>
  <action>
Refactor the existing sheet/dialog components so they all share the same mobile shell structure:
- full-screen dialog/backdrop layer
- bottom-aligned inner wrapper with side inset
- capped sheet height using `100dvh`
- `overflow-hidden` visual card
- internal scrolling body where needed
- visible action/footer area on longer forms

Apply the strictest footer/body split to `ItemDetailSheet.svelte` and `BarcodeLookupSheet.svelte`, where the action buttons must stay visible.

Preserve:
- backdrop dismiss
- close button dismiss
- current business logic and callbacks

Do not introduce a new library for sheets in this phase. Reuse native `dialog` + Tailwind.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>All mobile sheets use the same inset-card, capped-height, internal-scroll pattern and no sheet can render wider than the mobile viewport.</done>
</task>

</tasks>

<verification>
1. `npm run check`
2. Manual browser check at a narrow viewport: open item detail, category picker, and barcode lookup sheets and confirm no horizontal scroll appears.
3. Confirm action buttons in the long-form sheets remain visible without scrolling to the end of the content.
</verification>

<success_criteria>
- Protected layout clips horizontal overflow without breaking vertical scroll
- All existing bottom sheets are inset, capped, and mobile-safe
- ItemDetail and BarcodeLookup actions stay visible while sheet content scrolls internally
- No regressions to current close/backdrop behavior
</success_criteria>
